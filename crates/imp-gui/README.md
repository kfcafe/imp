# imp-gui

Native egui workbench for imp.

This crate starts as a standalone shell for the imp workbench UI: project status, mana work navigation, execution timeline, inspector, diff preview, and terminal output panes. Runtime integration will be added incrementally after the layout and app boundary are stable.

Run locally:

```sh
cargo run -p imp-gui
```

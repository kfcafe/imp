#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec cargo run -p imp-work --example migrate_mana_to_imp_work --manifest-path "$repo_root/Cargo.toml" -- "$@"

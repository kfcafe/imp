{
  "id": "stoppingcriteria",
  "source": "github.com/dirac-run/dirac evals/README.md master",
  "repo": "https://github.com/huggingface/transformers.git",
  "commit": null,
  "prompt": "Users have requested the ability to stop text generation early if the model becomes highly uncertain. Find the GenerationConfig class and add two new parameters: entropy_threshold: float | None = None and entropy_patience: int = 1. Next, locate the file containing the StoppingCriteria base class. In that same file, implement a new EntropyStoppingCriteria class that inherits from it. It should compute the Shannon entropy of the scores (logits) at each step. If the entropy exceeds entropy_threshold for entropy_patience consecutive steps, generation should stop. Wire this new criterion into the generation setup so it is activated when entropy_threshold is set.",
  "imp_command": "imp --provider $PROVIDER --model $MODEL --print < prompt.md",
  "verifier": "repo-specific ruff command TBD after pinning commit",
  "dirac_reference": {
    "patch": "evals/dirac-comparison/reference/dirac/dirac_refactor_stoppingcriteria",
    "metadata": "evals/dirac-comparison/reference/metadata/dirac/dirac_refactor_stoppingcriteria.json",
    "source_url": "https://raw.githubusercontent.com/dirac-run/dirac/master/evals/dirac/dirac_refactor_stoppingcriteria"
  }
}

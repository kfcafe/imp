---
id: '266.4.1.1'
title: Audit canonical imp run worker path and map hardening gaps
slug: audit-canonical-imp-run-worker-path-and-map-harden
status: open
priority: 1
created_at: '2026-04-22T03:43:37.965336Z'
updated_at: '2026-04-22T03:43:37.965336Z'
labels:
- vnext
- phase-0
- autonomy
- imp-run
- worker
- audit
parent: '266.4.1'
verify: cd /Users/asher/tower && mana show 266.4.1.1 >/dev/null
kind: job
---

Goal: produce an evidence-based gap map for the canonical `imp run <unit-id>` worker runtime.
Current state: repo docs and comments say `imp run` is the preferred single-unit worker path, implemented through `imp-core::mana_worker`, but there are known recent fixes around auth/provider defaults and the user does not yet trust the path.

Steps:
1. Inspect `imp/crates/imp-cli/src/lib.rs` run-command flow and trace how `Commands::Run` reaches `prepare_worker_run` / `finalize_worker_run`.
2. Inspect `imp/crates/imp-core/src/mana_worker.rs` and document the concrete stages: assignment loading, context assembly, session/runtime bootstrap, provider/model resolution, result finalization, verify/close integration, and failure handling.
3. Cross-check nearby docs/comments against code reality and call out any mismatches or transitional behavior still present.
4. List the highest-risk failure modes for autonomous use (e.g. auth resolution drift, empty/default prompt/instructions handling, weak result finalization, poor blocked-state reporting).
5. Propose the smallest hardening tasks that would materially increase trust in `imp run`.

Files:
- imp/crates/imp-cli/src/lib.rs (read — run command wiring)
- imp/crates/imp-core/src/mana_worker.rs (read — canonical worker runtime)
- imp/crates/imp-core/src/imp_session.rs (read — startup/runtime use from worker path if relevant)
- imp/crates/imp-core/src/system_prompt.rs (read — task packet shaping if relevant)
- imp/vNext.md (read — claimed direction)

Context:
- `imp run <unit-id>` is intended to be the canonical single-unit worker runtime.
- Recent repo notes mention fixes around direct imp run provider auth resolution and empty-string API key handling.
- The output should separate inspected evidence from inference.

In scope:
- audit and planning only
- concrete follow-up job proposals

Out of scope:
- changing code
- auditing `mana run` beyond what is necessary to understand `imp run` expectations

Do not:
- assume finalization/close behavior without reading it
- treat comments as proof if code differs
- broaden into UI concerns

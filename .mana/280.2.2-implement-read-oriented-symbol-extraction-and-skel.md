---
id: '280.2.2'
title: Improve imp scan for symbol extraction and skeleton-oriented context
slug: implement-read-oriented-symbol-extraction-and-skel
status: open
priority: 1
created_at: '2026-04-27T22:07:55.956750Z'
updated_at: '2026-04-28T01:31:29.941847Z'
acceptance: The existing `scan` tool exposes discoverable, compact skeleton/orientation output and `extract` supports documented `file#symbol`, `file:start-end`, and `file:line` targets. Tests prove Rust and TypeScript symbol extraction/skeleton behavior, existing scan behavior is preserved, and no separate model-facing symbol/skeleton tool is introduced.
notes: |2

  ## Attempt 1 — 2026-04-28T01:31:29Z
  Exit code: 1

  ```
  error: unexpected argument 'symbol' found

  Usage: cargo test [OPTIONS] [TESTNAME] [-- [ARGS]...]

  For more information, try '--help'.
  ```
labels:
- imp
- code-intelligence
- implementation
- read-tools
parent: '280.2'
dependencies:
- '280.2.1'
verify: cd /Users/asher/imp && rg -q 'symbol_extract|code_skeleton|symbol_skeleton' crates/imp-core/src/tools crates/imp-core/src/builder.rs && cargo test -p imp-core scan symbol skeleton -- --nocapture && cargo check -p imp-core
attempts: 1
history:
- attempt: 1
  started_at: '2026-04-28T01:31:29.889138Z'
  finished_at: '2026-04-28T01:31:29.941844Z'
  duration_secs: 0.052
  result: fail
  exit_code: 1
  output_snippet: |-
    error: unexpected argument 'symbol' found

    Usage: cargo test [OPTIONS] [TESTNAME] [-- [ARGS]...]

    For more information, try '--help'.
kind: task
autonomy_disposition:
  kind: blocked
  blockers:
  - verify_failed
  review: unknown
  approval: unknown
  verify: failed
  visibility: unknown
  attempt_pressure: within_budget
  risk: unknown
  provenance: mixed
  continuation_budget: 2
---

Goal: Improve imp's existing `scan` tool with Dirac-inspired read-oriented code intelligence. Keep this as an enhancement to the existing `scan` tool; do not add new model-facing tools unless later explicitly approved.

Current state:
- User wants continued movement toward tool parity with `github.com/dirac-run/dirac`.
- Durable direction is to improve `read`, `scan`, and `edit`; avoid separate new tools for the first slice.
- `crates/imp-core/src/tools/scan/mod.rs` already supports `scan`, `build`, and `extract` actions.
- Existing `scan extract` can target `file#symbol`, `file:start-end`, and `file:line`.
- Existing scan language modules include Rust, TypeScript/TSX, Python, and Go.
- The current verify command has drift: it still looks for names like `symbol_extract|code_skeleton|symbol_skeleton`, while the desired direction is to preserve one model-facing `scan` tool.

Planned decomposition:
1. Update the verify gate first so success proves upgraded `scan` behavior without requiring new model-facing tools.
2. Make skeleton-oriented output first-class inside existing `scan`/`build` behavior: compact file/module outlines with symbol kind, signature/name, and line ranges.
3. Promote symbol extraction inside existing `scan extract file#symbol`: schema/help text should make `file#symbol`, `file:start-end`, and `file:line` easy for the model to discover.
4. Add structured, provider-compatible details where useful: path, language, kind, symbol/name, start_line, end_line, and truncation status, while preserving human-readable output.
5. Keep output compact and model-actionable; use `rg`/shell for exact string search and `scan` for symbols, definitions, outlines, and coherent code blocks.
6. Add targeted tests for Rust and TypeScript fixtures proving skeleton/orientation output and symbol extraction behavior.

Files:
- `crates/imp-core/src/tools/scan/mod.rs` (primary implementation/schema/output)
- `crates/imp-core/src/tools/scan/types.rs` and language modules as needed
- `crates/imp-core/src/tools/mod.rs` only if registration metadata must change
- In-module or crate tests/fixtures for Rust and TypeScript

In scope:
- Existing `scan` tool improvements for skeleton/orientation and symbol extraction.
- Schema/help text improvements for action/target discoverability.
- Compact structured metadata in results when compatible.

Out of scope:
- New model-facing tools.
- References, rename, replace, anchor edits, diagnostics.
- Full LSP integration.
- Adding broad dependencies without approval.

Do not:
- Break existing `scan` action names or target formats.
- Regress current scan/build/extract behavior.
- Add separate `symbol_extract` or `code_skeleton` tools for this slice.

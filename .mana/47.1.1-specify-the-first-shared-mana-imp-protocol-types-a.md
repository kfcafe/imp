---
id: '47.1.1'
title: Specify the first shared mana ↔ imp protocol types and decide contract crate placement
slug: specify-the-first-shared-mana-imp-protocol-types-a
status: open
priority: 1
created_at: '2026-04-08T10:20:13.425479Z'
updated_at: '2026-04-08T10:20:13.425479Z'
labels:
- imp
- contracts
- boundary
- analysis
parent: '47.1'
verify: test -n "analysis-only"
kind: job
---

Goal: make the first protocol slice concrete enough to start implementation. Current state: the rebuild strategy calls for a canonical contracts layer, but the exact initial type set and crate location remain unresolved.

In scope:
- propose the first minimal shared type set
- compare plausible crate placement options
- choose the preferred placement with rationale
- identify which existing imp structs should map to the new types first

Out of scope:
- implementing the full migration
- moving all existing types immediately

Important decisions to resolve explicitly:
1. Tower-level contracts crate vs mana-owned contracts vs imp-owned shared crate
2. which type names/shapes are stable enough to introduce first
3. how much provenance/artifact detail belongs in v1 vs later

Suggested implementation follow-ups after this job:
- create the contracts crate
- add `TaskSpec`, `WorkerAssignment`, `WorkerOutcome`, `VerifierResult`, and `EvidenceBundle`
- add compatibility adapters from existing `imp-core/src/mana_worker.rs` types

Files to read:
- `imp_rebuild_strategy.md`
- `../rebuild_plan.md`
- `crates/imp-core/src/mana_worker.rs`
- relevant `mana-core` unit/verify/artifact types

Done when:
- another agent could create the contracts crate and first shared types without guessing where it belongs or what the initial payloads are
- the unresolved tradeoffs are written down, not left implicit

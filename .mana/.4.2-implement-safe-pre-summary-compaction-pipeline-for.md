---
id: '.4.2'
title: ''
slug: implement-safe-pre-summary-compaction-pipeline-for
status: closed
priority: 1
created_at: '2026-04-01T15:38:00.155249Z'
updated_at: '2026-04-23T13:26:12.969307Z'
notes: |-
  ---
  2026-04-01T15:59:39.096554+00:00
  Completed safe pre-summary compaction-prep pipeline.

  Implemented new `crates/imp-core/src/compaction.rs` and exported it from `imp-core`:
  - added `AssistantActionGroup` and deterministic assistant-boundary grouping
  - groups pull directly preceding user prompts into assistant-action ranges so preserved tails keep the prompting user context with the preserved assistant work
  - added `shrink_messages_for_summary()` to replace old tool-result bodies with lightweight placeholders preserving tool name / args summary / byte count
  - added `PreparedCompaction` and `prepare_messages_for_compaction()`
  - deterministic prep now returns:
    - `summary_input` for the future summarizer
    - `preserved_tail` kept verbatim
    - `preserved_tail_start`
    - discovered groups
    - count of shrunk tool results
  - integrated `sanitize_messages()` so both the reduced prefix and preserved tail remain safe with respect to tool-call/result invariants

  Added focused tests for:
  - assistant/user grouping behavior
  - preserved recent-group tail behavior
  - shrinking tool-heavy prefixes
  - sanitizing unpaired tool-call history
  - no-op behavior when history is too short

  Verification passed:
  - `cargo test -p imp-core context_compaction -- --nocapture`
  - `cargo check -p imp-core`

  Next: claim `.4.3` and build the real local/manual summarizer + compaction executor on top of this prep pipeline.

  ---
  2026-04-07T01:59:26.913326+00:00
  Implemented a substantial first `.4.2` slice in `mana-cli`.

  Landed in code:
  - added canonical `RunTarget` (`AllReady`, `Unit`, `Explicit`) in `crates/mana-cli/src/commands/run/mod.rs`
  - added `NativeRunParams`
  - `plan_dispatch` in `crates/mana-cli/src/commands/run/plan.rs` now accepts canonical targets instead of `Option<&str>`
  - `print_plan_json` now uses the canonical target scope label
  - loop mode now preserves the full canonical target across iterations instead of degrading it back to an `id`-only path
  - added/updated public embedding-oriented exports from `crates/mana-cli/src/lib.rs`: `run_native`, `run_with_stream_capture`, `run_with_stream_capture_and_sink`, `NativeRunParams`, `RunTarget`, `RunView`, etc.
  - `RunView` now includes structured `runtime` info (`direct_agent`, `model`)
  - dry-run human output now surfaces runtime information

  Verified in this session:
  - `cargo check -p mana-cli` ✅
  - `cargo test -p mana-cli plan_dispatch_target_parent_recurses_to_ready_leaf_descendants -- --nocapture` ✅
  - `cargo test -p mana-cli run_loop_target_subtree_drains_ready_descendants -- --nocapture` ✅
  - `cargo test -p mana-cli run_target_scope_label_formats_explicit_targets -- --nocapture` ✅

  Remaining `.4.2` work:
  - add/verify explicit-target-set dispatch test coverage fully (test filter for the new plan test returned 0 matches because the test discovery/filter name needs re-checking)
  - make the embedding path cleaner by reducing duplicate runner setup between `cmd_run`, `run_native`, and `run_with_stream_capture_and_sink`
  - continue `.4.2.3` by carrying richer runtime info into stream/dry-run/final result more comprehensively if needed

  ---
  2026-04-23T13:26:12.969300+00:00
  2026-04-23 resumed and reverified against current imp workspace state. The imp-core compaction-prep slice described in the unit notes is present and still passes the unit verify gate. Evidence: `cargo test -p imp-core context_compaction -- --nocapture && cargo check -p imp-core` passed; `crates/imp-core/src/compaction.rs` still contains the expected prep seam (`AssistantActionGroup`, `PreparedCompaction`, `assistant_action_groups`, `shrink_messages_for_summary`, `prepare_messages_for_compaction`) and the focused context-compaction tests remain green. The later mana-cli note in this unit appears to be unrelated cross-project noise; for the imp-scoped job, acceptance is satisfied and the unit can be closed.
labels:
- compaction
- context
- tools
- imp-core
parent: '.4'
dependencies:
- '.4.1'
verify: cd /Users/asher/tower/imp && cargo test -p imp-core context_compaction && cargo check -p imp-core
checkpoint: '3f47b01cbf424c2a5b61a7fa962f844b314d84e6'
verify_hash: b2895fb3f1f93c9c4aa8e86196316c873c874bb559468775b5a1bb9297a125b0
claimed_by: imp
claimed_at: '2026-04-01T15:54:54.477280Z'
kind: epic
attempt_log:
- num: 1
  outcome: abandoned
  agent: imp
  started_at: '2026-04-01T15:54:54.477280Z'
---

---
id: '45.4.2'
title: Plan the first imp-local runner adapter that consumes the Tower-level protocol
slug: plan-the-first-imp-local-runner-adapter-that-consu
status: open
priority: 1
created_at: '2026-04-08T17:33:36.024745Z'
updated_at: '2026-04-27T21:45:41.592656Z'
notes: |
  ---
  2026-04-09T13:20:38.754026+00:00
  Input protocol slice is now explicit in docs/rebuild/runner-protocol-surface.md. The adapter-plan unit should inherit these concrete groups rather than redefining them: Ensure/ReleaseWorktree, Ensure/ReleaseSandbox, Start/Wait-or-Observe/RunCommand, normalized stdout-stderr-artifact capture fields, and CancelExecution. Keep the adapter plan focused on where these shared contracts are consumed inside imp, how lease IDs flow through imp-local runtime code, and what stays adapter-local versus Tower-level.
  ## Attempt 1 — 2026-04-27T21:45:41Z
  Exit code: 1

  ```

  ```
labels:
- tower
- rebuild
- phase-3
- runner
- imp
- spec
parent: '45.4'
dependencies:
- '45.4.1'
verify: cd /Users/asher/imp && test -f docs/rebuild/imp-local-runner-adapter-plan.md && rg -q 'tower-contracts' docs/rebuild/imp-local-runner-adapter-plan.md && rg -q 'agent.rs' docs/rebuild/imp-local-runner-adapter-plan.md && rg -q 'tools/mana.rs' docs/rebuild/imp-local-runner-adapter-plan.md && rg -q 'mana_worker.rs' docs/rebuild/imp-local-runner-adapter-plan.md
attempts: 1
history:
- attempt: 1
  started_at: '2026-04-27T21:45:41.541920Z'
  finished_at: '2026-04-27T21:45:41.592653Z'
  duration_secs: 0.05
  result: fail
  exit_code: 1
kind: job
---

## Task
Write `docs/rebuild/imp-local-runner-adapter-plan.md` describing how the first local runner adapter should live inside `imp` while consuming the Tower-level runner protocol.

## Current inspected state
- User decision recorded in the graph: start the first local runner implementation inside `imp`.
- `imp/imp_rebuild_plan.md` says Phase I6 should introduce the runner-facing execution boundary.
- `imp/crates/imp-core/src/agent.rs`, `imp/crates/imp-core/src/tools/mana.rs`, and `imp/crates/imp-core/src/mana_worker.rs` are current hot spots around execution/runtime integration.

## Steps
1. Create `docs/rebuild/imp-local-runner-adapter-plan.md`.
2. Specify where the first local adapter should live inside `imp` conceptually.
3. Describe how it should consume shared runner protocol types without making those types `imp`-owned.
4. Map likely touchpoints in `imp-core` that will need adaptation later.
5. Keep the plan narrow and migration-oriented.

## Files
- `docs/rebuild/imp-local-runner-adapter-plan.md` (create)
- `imp/imp_rebuild_plan.md` (read only)
- `imp/crates/imp-core/src/agent.rs` (read only)
- `imp/crates/imp-core/src/tools/mana.rs` (read only)
- `imp/crates/imp-core/src/mana_worker.rs` (read only)

## Acceptance
- The note describes implementation placement inside `imp`.
- It describes protocol consumption from `tower-contracts`.
- It names concrete `imp-core` touchpoints for the later implementation slice.

## Don't
- Don't implement the adapter in this unit.
- Don't define the Tower-level protocol here; consume the protocol spec instead.
- Don't broaden into full imp decomposition.

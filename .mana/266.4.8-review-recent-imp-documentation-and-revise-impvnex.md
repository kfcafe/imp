---
id: '266.4.8'
title: Review recent imp documentation and revise imp/vNext.md accordingly
slug: review-recent-imp-documentation-and-revise-impvnex
status: open
priority: 1
created_at: '2026-04-21T04:05:28.390170Z'
updated_at: '2026-04-21T04:27:43.221664Z'
notes: |-
  ---
  2026-04-21T04:09:16.608121+00:00
  Reviewed recent imp docs and updated `imp/vNext.md` accordingly. Main deltas: elevated shared runtime/bootstrap consolidation into phase 0; made CLI-first/default product shape an explicit cross-cutting constraint; strengthened the canonical `imp run` + native `mana` orchestration split; reframed phase 4 around guest-runtime substrate + host-authoritative policy + script-tool separation; clarified mana/imp ownership that `mana` owns structured execution bundles while `imp` owns final prompt assembly and live execution. The Hermes comparison remains useful, but vNext is now more tightly aligned with current imp docs than before.

  ---
  2026-04-21T04:10:10.579766+00:00
  Durable follow-up from doc review discussion:

  Open discussion points to resolve before the next vNext refinement pass:
  1. Should phase 0 explicitly name shared runtime/bootstrap consolidation in the heading, not just the body?
  2. Should CLI-first remain a cross-cutting constraint or become its own top-level phase/track?
  3. Should canonical worker-runtime clarity (`imp run` + native `mana` orchestration split) be pulled even earlier and more explicitly into phase 0/1?
  4. Should phase 4 be renamed from `Extension and hostability` to something more specific such as `Guest-runtime substrate, SDK, and hostability` or `Host-authoritative extension and embedding seams`?

  Current recommendation from the review pass:
  - yes to (1)
  - keep CLI-first as a cross-cutting constraint rather than a separate top-level phase
  - yes to (3)
  - yes to (4)

  If accepted, the next patch to `imp/vNext.md` should tighten phase naming and ordering around these recommendations without changing the core ranked roadmap.

  ---
  2026-04-21T04:27:43.221659+00:00
  Updated `imp/vNext.md` per discussion. Tightened phase naming and ordering without changing the ranked roadmap: phase 0 now explicitly names shared runtime/bootstrap + worker-runtime preconditions; phase 4 is now `Guest-runtime substrate, SDK, and hostability`; adopt-next and implementation-lane language was updated to match; success criteria now explicitly mention guest-runtime seams alongside hostability and extension clarity.
labels:
- vnext
- docs
- review
- imp
parent: '266.4'
verify: cd /Users/asher/imp && test -f vNext.md && rg -q '^# imp vNext$' vNext.md && rg -q '## Ranked roadmap' vNext.md
kind: job
---

Review the recent imp architecture/product/runtime documentation and update `/Users/asher/imp/vNext.md` so the roadmap reflects the current documented direction rather than only the Hermes comparison. Focus on recently authored or recently active imp docs and proposals, especially those that affect sequencing, hostability, extension direction, CLI/TUI product direction, worker/runtime seams, and mana/imp ownership. Ground findings in inspected docs, discuss the implications, then patch `vNext.md` to align with the current documentation set while preserving the ranked roadmap. Keep edits focused to `vNext.md` unless a tiny discoverability tweak is clearly warranted.

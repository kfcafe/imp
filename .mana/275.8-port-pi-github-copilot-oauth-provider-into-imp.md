---
id: '275.8'
title: Port Pi GitHub Copilot OAuth provider into imp
slug: port-pi-github-copilot-oauth-provider-into-imp
status: open
priority: 3
created_at: '2026-04-27T18:16:13.438847Z'
updated_at: '2026-04-27T19:21:11.755154Z'
acceptance: 'imp supports GitHub Copilot OAuth/device flow and provider routing using Pi as reference, or a documented blocker explains why it is deferred. If implemented: login flow, refresh/token exchange, model enablement if needed, provider/model metadata, setup/model-picker UX, and targeted tests all exist.'
labels:
- imp
- oauth
- github-copilot
- pi-parity
parent: '275'
dependencies:
- '275.11'
verify: cargo test -p imp-llm github_copilot --lib && cargo test -p imp-cli github_copilot --lib && cargo test -p imp-tui github_copilot --lib && cargo check -p imp-cli -p imp-tui
kind: job
---

Goal: Port GitHub Copilot OAuth/provider support from Pi into imp after the planning units land.
Current state: Pi has GitHub Copilot OAuth in `/Users/asher/pi-mono/packages/ai/src/utils/oauth/github-copilot.ts`, including device code flow, optional GitHub Enterprise domain, Copilot token refresh, base URL extraction from `proxy-ep`, request headers, and model enablement. imp does not currently have a GitHub Copilot provider. This unit should be executed after 275.10/275.11 and preferably after simpler Google/Z.AI work unless sequence says otherwise.

Steps:
1. Read `imp/docs/plans/pi-provider-oauth-parity.md`, especially GitHub Copilot sequence/risk notes.
2. Inspect Pi references:
   - `/Users/asher/pi-mono/packages/ai/src/utils/oauth/github-copilot.ts`
   - `/Users/asher/pi-mono/packages/ai/src/providers/github-copilot-headers.ts` if present
   - `/Users/asher/pi-mono/packages/ai/src/models.generated.ts` github-copilot models
   - tests under `/Users/asher/pi-mono/packages/ai/test/*copilot*` for expected behavior.
3. Inspect imp auth/provider patterns:
   - `imp/crates/imp-llm/src/oauth/*`
   - `imp/crates/imp-llm/src/auth.rs`
   - `imp/crates/imp-llm/src/providers/openai.rs`, `openai_compat.rs`, `anthropic.rs` to decide routing
   - `imp/crates/imp-llm/src/model.rs`
   - `imp/crates/imp-cli/src/lib.rs`
   - `imp/crates/imp-tui/src/views/login_picker.rs`, setup/model picker surfaces.
4. Implement OAuth device flow:
   - prompt optional GitHub Enterprise URL/domain
   - request device code from `https://{domain}/login/device/code`
   - poll `https://{domain}/login/oauth/access_token`
   - exchange GitHub token for Copilot token via `copilot_internal/v2/token`
   - store refresh/access and enterprise domain without printing tokens.
5. Implement request/provider routing for the chosen initial model set. Use Pi headers and base URL extraction from `proxy-ep` if needed.
6. Decide model set from plan: start small with high-value OpenAI/Anthropic-backed Copilot models rather than importing all generated entries.
7. Integrate login/setup/model picker UX.
8. Add tests for device-flow response parsing/poll handling, token refresh parsing/base URL extraction, provider/model registry, setup visibility, and auth resolution.
9. Update docs/help for `imp login github-copilot` if added.

In scope:
- GitHub Copilot only.
- Optional Enterprise domain if straightforward from Pi.

Out of scope:
- Cursor support.
- Google/Z.AI providers.
- Broad model catalog generation.

Do not:
- Print tokens.
- Assume every Copilot model works; start with tested model metadata.
- Add dependencies unless approved.

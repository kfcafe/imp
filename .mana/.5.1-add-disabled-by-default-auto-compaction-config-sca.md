---
id: '.5.1'
title: Add disabled-by-default auto-compaction config scaffolding
slug: add-disabled-by-default-auto-compaction-config-sca
status: open
priority: 1
created_at: '2026-04-17T07:40:45.171837Z'
updated_at: '2026-04-17T08:04:37.774304Z'
notes: |-
  ---
  2026-04-17T08:02:41.204195+00:00
  Starting step 5 from the agreed order. Will inspect the existing config/context seams in `crates/imp-core/src/config.rs`, add disabled-by-default auto-compaction placeholder config only, and keep runtime behavior unchanged.

  ---
  2026-04-17T08:04:37.774298+00:00
  Implemented the disabled-by-default auto-compaction config scaffold in `crates/imp-core/src/config.rs` without changing runtime behavior.

  What changed:
  - Added `AutoCompactionMode` enum with placeholder variants:
    - `Disabled` (default)
    - `NearThreshold`
    - `Aggressive`
  - Added `AutoCompactionConfig { mode }`
  - Extended `ContextConfig` with `auto_compaction: AutoCompactionConfig` behind `#[serde(default)]`
  - Kept default behavior as manual-only by setting the default mode to `Disabled`
  - Added/updated config tests so TOML loading, merging, and defaults cover the new placeholder field

  Verification:
  - `cargo check -p imp-core`
  - `cargo test -p imp-core config_ -- --nocapture`
  Both passed.

  Important constraint preserved:
  - No automatic compaction trigger/plumbing was added. This is config scaffolding only, matching the settled policy.
labels:
- imp
- compaction
- config
- placeholder
parent: '.5'
verify: cd /Users/asher/tower/imp && rg -q 'auto_compaction|auto-compaction|AutoCompaction' crates/imp-core/src/config.rs && cargo check -p imp-core
kind: epic
---

Goal: add the minimal config/settings seam for future auto-compaction without enabling any automatic compaction behavior.

Current state:
- Manual `/compact` exists and `crates/imp-core/src/compaction.rs` already contains local compaction machinery.
- Root planning has now settled that automatic compaction stays disabled by default for now.
- `crates/imp-core/src/config.rs` currently has context masking settings (`ContextConfig`) but no explicit auto-compaction policy/config surface.

Steps:
1. Inspect `crates/imp-core/src/config.rs` and existing context/compaction types.
2. Add a small config shape for future auto-compaction policy/settings in `imp-core` config, with a default that preserves current behavior (manual only / disabled).
3. Wire serialization/defaults so existing configs continue to deserialize cleanly.
4. Do not trigger compaction anywhere yet; this job is config scaffolding only.
5. Add focused tests if the config module already has nearby coverage patterns; otherwise rely on compile verification.

Files:
- `crates/imp-core/src/config.rs` (modify — add placeholder auto-compaction policy/settings)
- `crates/imp-core/src/compaction.rs` (read — align naming only if needed; avoid behavior changes)

In scope:
- config/settings placeholder seam
- disabled-by-default behavior preservation

Out of scope:
- automatic triggering
- shared runner refactor
- TUI/runtime integration
- provider-native compaction

Do not:
- enable auto-compaction by default
- add background compaction behavior
- duplicate the current manual compaction execution path into a new runtime flow

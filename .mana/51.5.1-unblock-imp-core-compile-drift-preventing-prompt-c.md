---
id: '51.5.1'
title: Unblock imp-core compile drift preventing prompt-context verification
slug: unblock-imp-core-compile-drift-preventing-prompt-c
status: open
priority: 1
created_at: '2026-04-09T15:10:59.114633Z'
updated_at: '2026-04-13T20:45:38.775025Z'
notes: |-
  ---
  2026-04-09T15:11:44.398959+00:00
  Decomposition/evidence externalized from 51.5 verify attempt:
  - 51.5 implementation slice is locally complete in the working tree: `system_prompt::AssembleParams` now has distinct optional `project_memory_status`, `builder.rs` passes it separately from facts, and `mana_prompt_context.rs` formats a bounded preview block from `warnings` / `working_on` / `recent_work` via `mana_core::api::{find_mana_dir,memory_context}` with no-op degradation on missing `.mana` or load failure.
  - 51.5 verify still cannot run because `cargo test -p imp-core mana_prompt_context` and `cargo test -p imp-core system_prompt` both fail during crate compilation before reaching those targeted tests.
  - Exact blocker signatures from compiler output:
    1. `imp/crates/imp-core/src/agent.rs`: missing `finish_turn_mana_review`; stale `AgentEvent::TurnEnd { index, message }` patterns after `mana_review` field addition; stale `ToolContext` initializer missing `turn_mana_review`.
    2. `imp/crates/imp-core/src/imp_session.rs`: `AgentEvent::TurnEnd` initializers missing `mana_review`.
    3. `imp/crates/imp-core/src/session.rs`: stale `AgentEvent::TurnEnd` pattern missing `mana_review`.
    4. `imp/crates/imp-core/src/tools/{ask,bash,edit,extend,mana,memory,multi_edit,read,session_search,shell,web,write}.rs`: `ToolContext` initializers missing `turn_mana_review`.
    5. `imp/crates/imp-core/src/mana_review.rs`: `ManaTouchKind` and `ManaReviewScopeKind` need ordering support for `BTreeSet` usage (`Ord`/related derives).
  - Scope intent for this blocker remains narrow: restore `cargo check -p imp-core` so 51.5 targeted prompt-layer verification can proceed; do not reopen the prompt-layer design itself.

  ---
  2026-04-09T17:34:04.514008+00:00
  Observed while verifying root fact `53` (`Canonical worker handoff/result vocabulary now lives in tower-contracts::worker`): `cargo check -p imp-core` currently fails before that fact can fully verify.

  Current compile blockers to address in this unit:
  - `imp/crates/imp-core/src/agent.rs`: missing method `finish_turn_mana_review`
  - `imp/crates/imp-core/src/agent.rs`: `AgentEvent::TurnEnd { index, message }` patterns need to handle new `mana_review` field
  - `imp/crates/imp-core/src/agent.rs`: `ToolContext` initializer missing `turn_mana_review`
  - `imp/crates/imp-core/src/session.rs`: `AgentEvent::TurnEnd { index, message }` pattern also needs `mana_review` handling
  - `imp/crates/imp-core/src/mana_review.rs`: `ManaTouchKind` and `ManaReviewScopeKind` are used in `BTreeSet`/ordered collection paths but do not derive `Ord`

  This compile drift is currently blocking verify for unit `53` in addition to this unit's own `cargo check -p imp-core` gate.

  ---
  2026-04-13T20:45:38.775021+00:00
  Project-wide blocker review (2026-04-13): imp-core compile drift here is acting as a practical unblocker, not just a local verification task. It sits in front of prompt-context verification for 51.5 and is also relevant to completing fact 53 verification around tower-contracts worker vocabulary. Clearing this compile issue should be treated as a near-term unblock step.
labels:
- imp
- build
- verification
- blocker
parent: '51.5'
verify: cd /Users/asher/tower && cargo check -p imp-core
kind: job
---

Current state:
- Unit `51.5` has the intended prompt-layer changes in `imp/crates/imp-core/src/system_prompt.rs`, `imp/crates/imp-core/src/mana_prompt_context.rs`, and `imp/crates/imp-core/src/builder.rs`.
- Its verify gate cannot run yet because `cargo test -p imp-core ...` fails during compilation on unrelated existing drift outside those files.

Task:
Restore `imp-core` to a compilable state so targeted prompt-context verification can run again.

Concrete work clusters from the current compiler output:
1. `imp/crates/imp-core/src/agent.rs`
   - restore or reconcile `finish_turn_mana_review`
   - update `AgentEvent::TurnEnd` construction/patterns to account for `mana_review`
2. `imp/crates/imp-core/src/session.rs` and `imp/crates/imp-core/src/imp_session.rs`
   - propagate the current `AgentEvent::TurnEnd` shape consistently
3. `imp/crates/imp-core/src/tools/*.rs`
   - update stale `ToolContext` initializers to include `turn_mana_review`
4. `imp/crates/imp-core/src/mana_review.rs`
   - add the missing derives / ordering support required by `BTreeSet` usage (`ManaTouchKind`, `ManaReviewScopeKind`)

Files to inspect first:
- `imp/crates/imp-core/src/agent.rs`
- `imp/crates/imp-core/src/session.rs`
- `imp/crates/imp-core/src/imp_session.rs`
- `imp/crates/imp-core/src/tools/mod.rs`
- `imp/crates/imp-core/src/tools/ask.rs`
- `imp/crates/imp-core/src/tools/bash.rs`
- `imp/crates/imp-core/src/tools/mana.rs`
- `imp/crates/imp-core/src/mana_review.rs`

Acceptance:
- `cargo check -p imp-core` passes again.
- The unblocker fixes only the compile drift needed to restore verification flow; it should not reopen prompt-layer design questions from `51.5`.

Do not:
- Do not remove the new `project_memory_status` prompt seam from `51.5`.
- Do not mix prompt-layer logic with unrelated runtime redesign.
- Do not broaden into workspace-wide cleanup once `imp-core` compiles.

---
id: '280.1'
title: Run Dirac evals with imp using Gemini secret
slug: run-dirac-evals-with-imp-using-gemini-secret
status: open
priority: 3
created_at: '2026-04-27T21:02:31.255151Z'
updated_at: '2026-04-27T21:42:54.812902Z'
acceptance: 'A concise benchmark report exists in mana notes or a local artifact with: eval names, repo/task source, command used, model/provider, success/failure, verifier output, cost/token data if available, and blockers. No secrets printed.'
notes: |-
  ---
  2026-04-27T21:02:38.429205+00:00
  Started execution. This is not web performance benchmarking despite skill name; using benchmark discipline for evidence capture. Plan: inspect Dirac eval definitions/harness, identify public task repos and verify commands, then run imp with Gemini secret via imp CLI in isolated temp workdirs without printing secrets.

  ---
  2026-04-27T21:03:44.994809+00:00
  Blocker: attempted secret-backed smoke command `cargo run -q -p imp-cli -- --provider google --model gemini-2.5-flash --thinking high --max-turns 1 --print "Reply with OK"` from /Users/asher/imp. It failed with `Auth failed for google: No secret field 'api_key' found for google. Set GOOGLE_API_KEY or run imp login google`. I did not print or inspect secrets. Need either the correct provider name/secret location or env var/session secret exposure before benchmark runs can proceed.

  ---
  2026-04-27T21:12:04.668416+00:00
  User reports `imp secrets show google` lists the secret, but provider auth still looks for google field `api_key`. Pivoting briefly to debug imp's Google secret resolution because it blocks Dirac eval execution. User requested flash 3 preview; need verify available model naming after auth path works.

  ---
  2026-04-27T21:13:30.911615+00:00
  Root cause for Google auth blocker: `imp secrets show google` only confirms auth metadata in `~/.imp/auth.json`, not that the OS keychain value exists. Metadata contains `google: { type: SecretFields, fields: [api_key] }`, but direct keychain probe `security find-generic-password -s imp -a google:api_key` reports no item. `AuthStore::resolve_secret_field` then correctly returns missing_secret_error when keychain get returns None. Secondary blocker for requested model: imp model registry currently lists only `gemini-2.5-pro` and `gemini-2.5-flash`; flash 3 preview is not registered yet.

  ---
  2026-04-27T21:27:31.987430+00:00
  Related secrets blocker has been diagnosed and addressed in bug 136 implementation. `imp secrets show google` previously only showed metadata; after the patch it checks the secure storage value and reports `broken (api_key:missing)` with a non-zero exit. Local `target/debug/imp secrets doctor` reports google broken because the keychain value is missing even though metadata exists. To continue this benchmark after the patch lands/builds, re-save Google with `imp secrets google`, then rerun the smoke command. Secondary blocker remains: requested Gemini flash 3 preview is not registered in the model registry yet; current registered Google models include gemini-2.5-pro and gemini-2.5-flash.
  ## Attempt 1 Failed (40m21s, 0 tokens, $0.000)

  ### What was tried

  - 0 tool calls over 0 turns in 40m21s

  ### Why it failed

  - Cannot close as complete because its acceptance requires running Dirac evals and producing a benchmark report. Current blocker is now accurately diagnosed: local Google secret metadata exists but the secure keychain value is missing, so Gemini auth cannot run until the user re-saves Google (`imp secrets google`) or provides GOOGLE_API_KEY. Secondary model registry blocker remains for requested Gemini flash 3 preview; use registered `gemini-2.5-flash` unless adding the preview model is separately approved.

  ### Verify command

  `Review produced report and confirm all attempted evals include commands and pass/fail evidence.`
labels:
- benchmark
- dirac-comparison
- imp
parent: '280'
dependencies:
- '266.4.3.4'
verify: Review produced report and confirm all attempted evals include commands and pass/fail evidence.
checkpoint: '16eee3438bee47c5773416907bd12d49e43f8c3f'
verify_hash: e86ad63eb0ddfeea9537396ec9a5bb805fa683266cb48201df7b0d90fb245dbe
kind: job
attempt_log:
- num: 1
  outcome: failed
  notes: 'Cannot close as complete because its acceptance requires running Dirac evals and producing a benchmark report. Current blocker is now accurately diagnosed: local Google secret metadata exists but the secure keychain value is missing, so Gemini auth cannot run until the user re-saves Google (`imp secrets google`) or provides GOOGLE_API_KEY. Secondary model registry blocker remains for requested Gemini flash 3 preview; use registered `gemini-2.5-flash` unless adding the preview model is separately approved.'
  agent: imp
  started_at: '2026-04-27T21:02:33.009701Z'
  finished_at: '2026-04-27T21:42:54.812902Z'
---

Benchmark imp against Dirac's public eval tasks using the Gemini API key already loaded into imp secrets. Scope: inspect Dirac eval definitions, determine runnable task set and verifier commands, run imp as the agent on those tasks where possible, capture success/failure, cost/token data if available, diffs, and verifier evidence. Do not expose secrets. Do not modify imp source. Use temporary worktrees/checkouts for benchmark repositories. If eval definitions require Dirac-specific harness assumptions, document the blocker and produce an adapted runnable plan.

---
id: '45.5.3'
title: Write a compact imp decomposition order for post-contracts, post-runner migration
slug: write-a-compact-imp-decomposition-order-for-post-c
status: open
priority: 1
created_at: '2026-04-08T17:33:36.051081Z'
updated_at: '2026-04-09T13:22:01.120610Z'
notes: |-
  ---
  2026-04-09T12:35:14.805957+00:00
  Use the completed 45.5.2 worker-boundary spec as an ordering anchor. Repo-grounded follow-on discovered while writing the spec: `imp-cli/src/main.rs` still contains stale `#[cfg(test)]` duplicate mana-unit parsing helpers (`ManaUnit`, `load_mana_unit`, `find_mana_dir`, `parse_mana_unit`), while `run_headless_mode()` already uses `imp_core::mana_worker::*` as the canonical path. Also, a repo scan found no current direct `imp_core::mana_worker::WorkerAssignment|WorkerAttempt|WorkerResult|WorkerStatus` call sites, so the immediate concrete decomposition slice after the spec is parser-helper quarantine/cleanup in `imp-cli`, not a broad contracts-callsite migration. Sequence 45.5.3 accordingly.

  ---
  2026-04-09T13:22:01.120609+00:00
  Execution delta to sequence explicitly in the decomposition-order note: 45.5.4 is now completed and should be treated as the first landed structural cleanup after the worker-boundary spec. After this change, the canonical repo reading is stronger: `imp-cli` no longer presents a shadow mana-unit parser path, and local CLI coverage should prefer `imp_core::mana_worker` helpers over test-only duplicate parsing models. Sequence the remaining post-contracts work from that state rather than from the earlier mixed ownership state.
labels:
- tower
- rebuild
- phase-4
- imp
- migration
parent: '45.5'
dependencies:
- '45.5.2'
- '45.4.3'
verify: cd /Users/asher/tower && test -f docs/rebuild/imp-decomposition-order.md && rg -q 'contracts' docs/rebuild/imp-decomposition-order.md && rg -q 'runner' docs/rebuild/imp-decomposition-order.md && rg -q 'agent.rs' docs/rebuild/imp-decomposition-order.md && rg -q 'structural' docs/rebuild/imp-decomposition-order.md && rg -q 'behavior-sensitive' docs/rebuild/imp-decomposition-order.md
kind: job
---

## Task
Write `docs/rebuild/imp-decomposition-order.md` sequencing the first safe extraction order inside `imp` after contracts and runner prerequisites land.

## Current inspected state
- `imp/imp_rebuild_plan.md` gives the intended order: shared contracts, worker boundary cleanup, `agent.rs` decomposition, tools decomposition, context/session separation, runner-boundary integration, policy hardening, then evidence-oriented UX cleanup.
- Future workers need a compact decomposition sequence that is easier to execute than the full plan file.

## Steps
1. Create `docs/rebuild/imp-decomposition-order.md`.
2. Convert the Phase 4 order into a concise migration sequence.
3. Mention prerequisites from Phase 1 and Phase 3.
4. Identify which slices are mostly structural versus behavior-sensitive.
5. Keep it execution-oriented.

## Files
- `docs/rebuild/imp-decomposition-order.md` (create)
- `imp/imp_rebuild_plan.md` (read only)

## Acceptance
- The note lists the intended decomposition order.
- It references contracts and runner prerequisites.
- It distinguishes structural slices from behavior-sensitive slices.

## Don't
- Don't add new phases.
- Don't implement code here.
- Don't broaden into policy hardening details beyond sequence context.

---
id: '259.1'
title: Replace obvious production unwrap/unreachable sites with recoverable errors
slug: replace-obvious-production-unwrapunreachable-sites
status: closed
priority: 1
created_at: '2026-04-15T02:50:00.401885Z'
updated_at: '2026-04-15T02:52:19.455335Z'
notes: |-
  ---
  2026-04-15T02:52:19.455328+00:00
  Implemented first cleanup pass. Replaced interactive continue-session fallback unwrap in `imp-cli/src/lib.rs` with proper `Result` propagation when creating a new session if no recent session exists. Replaced `child.stdout.take().unwrap()` and `child.stderr.take().unwrap()` in `imp-core/src/tools/bash.rs` with recoverable `Error::Tool(...)` branches. Replaced `_ => unreachable!()` in `imp-core/src/tools/extend.rs` with an explicit user-facing error branch. Verification: `cargo test -p imp-cli` passed; focused `cargo test -p imp-core tools::bash::tests` and `cargo test -p imp-core tools::extend::tests` passed. Full `cargo test -p imp-core` still has 4 pre-existing unrelated failures in builder/config/mana_prompt_context/tools::imp tests.
labels:
- cleanup
- panic
- error-handling
- imp
parent: '259'
verify: cd /Users/asher/imp && cargo test -p imp-cli && cargo test -p imp-core
checkpoint: '9e044bfdfe03769494318cdd2364fd97dcf585cd'
verify_hash: '164a031da3f7105a118f34acd33b0bca05fe6a0fd4ba2a26df1e329a2be5553a'
claimed_by: imp
claimed_at: '2026-04-15T02:50:04.761648Z'
kind: job
attempt_log:
- num: 1
  outcome: abandoned
  agent: imp
  started_at: '2026-04-15T02:50:04.761648Z'
---

Implement the first cleanup pass from the panic/error-handling audit. Replace obvious production-only `unwrap`/`unreachable!` usage with recoverable errors or explicit handling, without broad architectural changes.

Targets:
1. `/Users/asher/imp/crates/imp-cli/src/lib.rs`
   - In interactive session setup, remove `unwrap_or_else(|| SessionManager::new(...).unwrap())` and propagate a normal error instead.
2. `/Users/asher/imp/crates/imp-core/src/tools/bash.rs`
   - Replace `child.stdout.take().unwrap()` and `child.stderr.take().unwrap()` with explicit error handling that returns a tool/runtime error if piped handles are unexpectedly absent.
3. `/Users/asher/imp/crates/imp-core/src/tools/extend.rs`
   - Replace `_ => unreachable!()` in the action match with an explicit error branch, even if the case is logically impossible after validation.

Constraints:
- Keep changes small and local.
- Preserve existing behavior on success.
- Prefer existing error types/patterns in each crate.
- Add or adjust focused tests only if needed to verify the new behavior.

Verify:
- `cd /Users/asher/tower && cargo test -p imp-cli && cargo test -p imp-core`
- If full imp-core is too broad/slow due to unrelated failures, run the narrowest relevant tests for cli + bash/extend tools and report results precisely.

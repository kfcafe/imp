---
id: '264.3.2'
title: Migrate config, auth, session, and session-search callsites to shared imp storage helpers
slug: migrate-config-auth-session-and-session-search-cal
status: open
priority: 1
created_at: '2026-04-16T10:20:47.076023Z'
updated_at: '2026-04-24T05:37:22.582441Z'
notes: |-
  ---
  2026-04-16T10:31:45.823934+00:00
  Started caller migration on top of the new shared storage module.

  Implemented so far:
  - `imp_session.rs`
    - auth path now resolves via explicit override -> legacy compatibility discovery -> canonical `~/.imp/auth.json`
    - session persistence now uses canonical `~/.imp/sessions/`
  - `tools/session_search.rs`
    - session index path now prefers canonical `~/.imp/indexes/session_index.db`
    - falls back to legacy compatibility roots before defaulting to canonical path
  - `tools/memory.rs`
    - memory/user paths now use canonical `~/.imp/memory.md` and `~/.imp/user.md`
  - `tools/extend.rs`
    - agent-created skills now target canonical `~/.imp/skills/agent/`
  - `tools/web/search.rs`
    - auth resolution now prefers explicit override -> legacy compatibility discovery -> canonical `~/.imp/auth.json`

  Current state:
  - `cargo check -p imp-core` passes after these path updates.
  - I paused before continuing broader CLI/TUI callsite migration because the user explicitly requested durable plan externalization mid-flight.

  Next steps for this unit:
  1. remove any small warnings/unused leftovers from the imp-core path migration slice
  2. run focused imp-core tests for the touched modules where available
  3. then decide whether to continue into imp-cli/imp-tui path callsites in this same unit or split further if the surface gets noisy

  Known unrelated failure not caused by this work:
  - `cargo test -p imp-core config -- --nocapture` still fails on `config::tests::agent_mode_planner_blocks_mana_close` due to pre-existing planner/update expectation drift.

  ---
  2026-04-16T10:37:32.044506+00:00
  Completed the current caller-migration slice and verified the touched crates/modules.

  What changed:
  - imp-core
    - `imp_session.rs`: auth path now resolves as explicit override -> legacy compatibility auth file -> canonical `~/.imp/auth.json`; session persistence now uses canonical `~/.imp/sessions/`
    - `tools/session_search.rs`: session index path now prefers canonical `~/.imp/indexes/session_index.db`, with fallback to legacy data roots before defaulting to canonical
    - `tools/memory.rs`: `memory`/`user` stores now target canonical `~/.imp/memory.md` and `~/.imp/user.md`
    - `tools/extend.rs`: agent-created skills now target canonical `~/.imp/skills/agent/`
    - `tools/web/search.rs`: auth path now prefers compatibility-discovered auth metadata before canonical `~/.imp/auth.json`
  - imp-cli
    - auth/config/session callsites switched to shared canonical storage helpers (`global_auth_path`, `global_config_path`, `global_sessions_dir`, `global_root` for resolve)
  - imp-tui
    - auth/config/session callsites switched to shared canonical storage helpers (`global_auth_path`, `global_config_path`, `global_sessions_dir`)

  Verification:
  - `cd /Users/asher/tower && cargo check -p imp-core && cargo check -p imp-cli && cargo check -p imp-tui` passes
  - `cargo test -p imp-core imp_session -- --nocapture` passes
  - `cargo test -p imp-core resources -- --nocapture` passes
  - `cargo test -p imp-core session_search_tool -- --nocapture` passes
  - `cargo test -p imp-core memory_tool -- --nocapture` passes
  - `cargo test -p imp-core extend_ -- --nocapture` passes

  Known unrelated failure still present elsewhere:
  - `cargo test -p imp-core config -- --nocapture` still fails on the pre-existing planner mana-action expectation drift (`agent_mode_planner_blocks_mana_close` asserting planner should block `update` despite current config allowing it)

  Next logical implementation step:
  - move to `264.3.3` and migrate canonical instruction discovery to `.imp/agents.md` while preserving top-level `AGENTS.md` compatibility.
labels:
- imp
- storage
- implementation
parent: '264.3'
dependencies:
- '264.3.1'
verify: cd /Users/asher/tower && cargo check -p imp-core && cargo check -p imp-cli && cargo check -p imp-tui
checkpoint: '3bc1c847bdc92396c3cee7474c95231937f3178a'
verify_hash: a4d67804b385871bced05818fab1f06c1efc50cc2970ac81da2cb6dbaa557e49
kind: job
attempt_log:
- num: 1
  outcome: abandoned
  agent: imp
  started_at: '2026-04-16T10:34:50.576605Z'
  finished_at: '2026-04-24T05:37:22.582441Z'
---

Goal: migrate the highest-value storage callsites to the new shared imp-core storage-path module while preserving legacy compatibility.

Current state:
- many callers currently join `Config::user_config_dir()`, `Config::session_dir()`, or custom paths directly
- accepted canonical roots are `~/.imp/` and `<project>/.imp/`

Steps:
1. Update config/auth/session/session-index callsites to use the shared storage helpers.
2. Keep compatibility lookup behavior in place so existing data remains discoverable.
3. Update the session/session-search path relationship so canonical raw sessions and canonical index live under `~/.imp/`.
4. Add focused tests around the migrated helpers/callers.

Relevant files likely include:
- `imp/crates/imp-core/src/imp_session.rs`
- `imp/crates/imp-core/src/tools/session_search.rs`
- `imp/crates/imp-core/src/tools/memory.rs`
- `imp/crates/imp-core/src/tools/extend.rs`
- `imp/crates/imp-core/src/tools/web/*`
- `imp/crates/imp-cli/src/lib.rs`
- `imp/crates/imp-tui/src/app.rs`

In scope:
- highest-value path callsites
- non-breaking canonicalization

Out of scope:
- full resource-discovery rewrite
- docs updates

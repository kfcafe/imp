---
id: '45.5.1'
title: Map imp-core hotspots into target runtime, context, tools, policy, and worker seams
slug: map-imp-core-hotspots-into-target-runtime-context
status: open
priority: 1
created_at: '2026-04-08T17:33:36.039360Z'
updated_at: '2026-04-27T21:45:41.705902Z'
notes: |2

  ## Attempt 1 — 2026-04-27T21:45:41Z
  Exit code: 1

  ```

  ```
labels:
- tower
- rebuild
- phase-4
- imp
- spec
parent: '45.5'
dependencies:
- '45.2.3'
- '45.4.2'
verify: cd /Users/asher/imp && test -f docs/rebuild/imp-runtime-hotspot-map.md && rg -q 'agent.rs' docs/rebuild/imp-runtime-hotspot-map.md && rg -q 'tools/mod.rs' docs/rebuild/imp-runtime-hotspot-map.md && rg -q 'session.rs' docs/rebuild/imp-runtime-hotspot-map.md && rg -q 'tools/mana.rs' docs/rebuild/imp-runtime-hotspot-map.md && rg -q 'config.rs' docs/rebuild/imp-runtime-hotspot-map.md && rg -q 'mana_worker.rs' docs/rebuild/imp-runtime-hotspot-map.md
attempts: 1
history:
- attempt: 1
  started_at: '2026-04-27T21:45:41.650887Z'
  finished_at: '2026-04-27T21:45:41.705901Z'
  duration_secs: 0.055
  result: fail
  exit_code: 1
kind: job
---

## Task
Write `docs/rebuild/imp-runtime-hotspot-map.md` mapping the currently inspected `imp-core` hotspots into the target runtime/context/tools/policy/worker seams.

## Current inspected state
- `imp/crates/imp-core/src/agent.rs`, `tools/mod.rs`, `session.rs`, `tools/mana.rs`, `config.rs`, and `mana_worker.rs` are the hot spots called out in `imp_rebuild_plan.md`.
- Phase 4 should rebuild `imp` around stable worker/runtime seams, but workers need a grounded seam map before code moves begin.

## Steps
1. Create `docs/rebuild/imp-runtime-hotspot-map.md`.
2. Map each inspected hotspot to the target seam it should move toward.
3. Identify which hotspots are ownership moves versus behavioral refactors.
4. Recommend the safest extraction order.
5. Keep it grounded in the inspected files, not generic architecture language.

## Files
- `docs/rebuild/imp-runtime-hotspot-map.md` (create)
- `imp/crates/imp-core/src/agent.rs` (read only)
- `imp/crates/imp-core/src/tools/mod.rs` (read only)
- `imp/crates/imp-core/src/session.rs` (read only)
- `imp/crates/imp-core/src/tools/mana.rs` (read only)
- `imp/crates/imp-core/src/config.rs` (read only)
- `imp/crates/imp-core/src/mana_worker.rs` (read only)

## Acceptance
- The note maps all six inspected hotspots.
- It distinguishes ownership-cleanup vs behavior-changing work.
- It recommends an extraction order future workers can follow.

## Don't
- Don't move code in this unit.
- Don't redesign the TUI here.

---
id: '27.3'
title: 'imp agent loop: non-blocking mana follow-ups and message delivery'
slug: imp-agent-loop-non-blocking-mana-follow-ups-and-me
status: closed
priority: 1
created_at: '2026-03-25T17:08:04.143382Z'
updated_at: '2026-04-23T05:29:57.738918Z'
acceptance: Background mana work returns promptly instead of monopolizing the turn; user messages and steering still reach the active agent while mana work exists in the background; completion arrives through a real follow-up or notification path; tests cover the non-blocking behavior.
notes: |-
  ---
  2026-04-17T07:22:45.519909+00:00
  Settled UX default from root planning: longer-running mana work should complete through both surfaces — (1) a status/notification signal and (2) an inline assistant follow-up / delivery path — while still returning promptly and allowing fresh user messages to reach the active agent.

  ---
  2026-04-17T07:30:02.261585+00:00
  Architecture clarification from root planning: background mana follow-ups/message delivery belong to the native Rust worker/runtime path for imp execution. This should not depend on Lua extension routing.

  ---
  2026-04-17T07:48:54.079441+00:00
  Starting step 2 from the agreed near-term implementation order while step 1 waits on an optional live provider smoke. Will run the unit verify gate, then inspect the current follow-up/delivery path in `imp-core`, `imp-cli`, and `imp-tui` to see how much of the non-blocking behavior is already present in the workspace state.

  ---
  2026-04-17T07:51:21.520507+00:00
  Ran the unit verify gate and targeted follow-up tests against the current workspace state:
  - verify command passed: `cargo test -p imp-core follow_up ... && cargo check -p imp-cli && cargo check -p imp-tui`
  - targeted follow-up tests passed:
    - `agent_follow_up_runs_after_current_work_finishes`
    - `background_run_enqueues_follow_up_on_completion_without_ui`
    - `background_run_with_ui_does_not_enqueue_follow_up_on_completion`
  Inspected current behavior:
  - `agent.rs` already drains `AgentCommand::FollowUp` between turns without blocking the active turn and queues follow-ups until current work finishes.
  - `tools/mana.rs` background native mana orchestration already does both parts of the settled UX: it emits `ui.notify(...)` / status/widget updates, and when there is no UI it enqueues an inline `AgentCommand::FollowUp(...)` summary on completion or failure.
  - `imp-tui/src/app.rs` consumes queued follow-ups at `AgentEnd` and sends them as the next message while preserving completion-notification behavior.
  Conclusion: the intended non-blocking + dual-surface delivery behavior appears already present in code/tests in this workspace. No new code change needed before moving down the ordered stack.

  ---
  2026-04-23T05:29:57.738914+00:00
  2026-04-23 resumed and reverified against current workspace state. The unit verify gate still passes unchanged: `cargo test -p imp-core follow_up ...`, `cargo check -p imp-cli`, and `cargo check -p imp-tui`. Evidence remains consistent with the prior inspection: background mana work already returns promptly, `AgentCommand::FollowUp` is queued/delivered between turns, non-UI background completion enqueues inline follow-up delivery, UI-backed runs surface status/notification without monopolizing the active turn, and user messages/steering remain covered by the existing follow-up tests. No new code change was needed; acceptance is satisfied in the current state.
labels:
- mana
- agent-loop
- imp-core
- imp-cli
- imp-tui
parent: '27'
dependencies:
- '27.1'
verify: cd /Users/asher/tower && cargo test -p imp-core follow_up 2>&1 | grep -q "test result:" && cargo check -p imp-cli && cargo check -p imp-tui
checkpoint: '3bc286ef534cce60dbae448e74cda27ffac1083f'
claimed_by: imp
claimed_at: '2026-03-29T21:54:40.036643Z'
kind: job
paths:
- crates/imp-core/src/agent.rs
- crates/imp-cli/src/main.rs
- crates/imp-tui/src/app.rs
attempt_log:
- num: 1
  outcome: abandoned
  agent: imp
  started_at: '2026-03-29T21:54:40.036643Z'
---

Make native mana runs non-blocking in the same way Pi's mana tool is: the tool should be able to kick off longer-running mana work, return promptly, and later surface completion without preventing fresh user messages from reaching the agent. Audit current handling of queued follow-ups and AgentCommand::FollowUp in imp-core, imp-cli, and imp-tui, replace the current dead-end behavior with a real completion-notification path, ensure background mana activity does not monopolize the active turn, and add regression tests.

---
id: '266.4.1.3'
title: Define proving matrix for imp run and mana run autonomous execution flows
slug: define-proving-matrix-for-imp-run-and-mana-run-aut
status: open
priority: 1
created_at: '2026-04-22T03:44:09.709736Z'
updated_at: '2026-04-22T03:44:09.709736Z'
labels:
- vnext
- phase-0
- autonomy
- evals
- runtime
- verification
parent: '266.4.1'
verify: cd /Users/asher/tower && mana show 266.4.1.3 >/dev/null
kind: job
---

Goal: define a small repeatable proving matrix for autonomous execution paths so imp/mana runtime fixes can be verified systematically.
Current state: runtime-path confidence is mostly anecdotal. There are targeted tests around pieces of the worker path, but no compact source-of-truth matrix describing the minimum scenarios that must pass for `imp run` and `mana run` to be considered trustworthy for autonomous work.

Steps:
1. Inventory the existing targeted tests and verify commands that already cover worker/orchestration behavior.
2. Define the minimum scenario matrix, including at least: successful `imp run` single-unit execution; provider/auth resolution under headless worker defaults; verify/close path; failure/blocked path; `mana run` direct-mode dispatch into `imp run`; log visibility; and run-state/reporting behavior.
3. For each scenario, specify the narrowest reliable verify command or harness approach, avoiding broad whole-project checks where a focused proof is possible.
4. Call out missing test seams or harness gaps that need follow-on work.
5. Sequence the first implementation/testing jobs needed to make the matrix executable in CI or local development.

Files:
- imp/crates/imp-core/src/mana_worker.rs (read for relevant tests or seams)
- imp/crates/imp-core/src/tools/mana.rs (read for run-state/log tests)
- mana/crates/mana-cli/src/commands/run/mod.rs (read for orchestration tests)
- imp/vNext.md (read — phase positioning)
- any nearby docs/tests discovered during audit

In scope:
- proving/eval design for autonomous runtime paths
- follow-up job identification

Out of scope:
- implementing the tests immediately unless a tiny proving slice is obvious later

Do not:
- propose weak verify gates like generic `cargo test`
- mix unrelated product eval ambitions into this runtime-specific matrix

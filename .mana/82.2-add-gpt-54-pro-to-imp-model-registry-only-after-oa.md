---
id: '82.2'
title: Add GPT-5.4-pro to imp model registry only after OAuth backend acceptance is confirmed
slug: add-gpt-54-pro-to-imp-model-registry-only-after-oa
status: open
priority: 2
created_at: '2026-04-13T05:12:23.705407Z'
updated_at: '2026-04-13T05:12:23.729586Z'
labels:
- imp
- openai
- oauth
- implementation
- models
parent: '82'
dependencies:
- '82.1'
verify: cd /Users/asher/imp && rg -q 'gpt-5.4-pro' crates/imp-llm/src/model.rs && cargo test -p imp-llm model -- --nocapture && cargo check -p imp-cli -p imp-tui
kind: job
---

Goal: if and only if the OAuth-backed backend accepts `gpt-5.4-pro`, expose it cleanly in imp's model registry and selection paths.

Current state:
- `imp/crates/imp-llm/src/model.rs` defines built-in OpenAI model metadata and aliases.
- `builtin_openai_codex_models()` mirrors OpenAI models onto provider `openai-codex`.
- CLI/TUI provider resolution already know how to route OpenAI-family requests onto `openai-codex` when only ChatGPT OAuth is present.

Steps:
1. Add `gpt-5.4-pro` model metadata in the appropriate built-in model list(s).
2. Decide whether the model should appear under both `openai` and `openai-codex` metadata or only the OAuth-backed surface, based on confirmed backend/API support.
3. Add alias coverage if helpful.
4. Add focused tests for model lookup/provider support to prevent regressions.
5. Verify with the smallest relevant cargo tests/checks.

Files:
- `imp/crates/imp-llm/src/model.rs` (modify — model metadata and aliases)
- `imp/crates/imp-cli/src/main.rs` (read/possibly modify only if provider support logic needs adjustment)
- `imp/crates/imp-tui/src/app.rs` (read/possibly modify only if provider support logic needs adjustment)

Do not:
- expose the model without backend confirmation
- broaden support claims beyond what was actually verified

Dependency: this work is blocked on the probe confirming backend acceptance.

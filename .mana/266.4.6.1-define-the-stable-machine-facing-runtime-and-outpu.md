---
id: '266.4.6.1'
title: Define the stable machine-facing runtime and output seam for imp hosts
slug: define-the-stable-machine-facing-runtime-and-outpu
status: open
priority: 1
created_at: '2026-04-21T02:52:48.045295Z'
updated_at: '2026-04-21T02:52:48.045295Z'
labels:
- vnext
- phase-4
- sdk
- runtime
- contracts
parent: '266.4.6'
verify: cd /Users/asher/imp && mana show 266.4.6.1 >/dev/null
kind: job
---

Goal: define the stable machine-facing runtime/event/output seam imp should expose to hosts and tools.

Current state:
- imp already has promising embedding/runtime pieces: `ImpSession`, SDK exports, CLI/TUI runtime events, and ongoing work on human vs machine output contracts.
- Hermes demonstrates the value of explicit protocol boundaries (TUI gateway, ACP adapter), but imp should align this with its existing Rust-native architecture.

Steps:
1. Inspect:
   - `/Users/asher/imp/crates/imp-core/src/sdk.rs`
   - `/Users/asher/imp/crates/imp-core/src/imp_session.rs`
   - `/Users/asher/imp/crates/imp-core/src/agent.rs`
   - related output/runtime contract docs already in Tower mana graph (e.g. 50.14 / 50.16.1 / 248.18.3)
2. Specify the minimal stable machine-facing seam that vNext should standardize.
3. Cover:
   - event taxonomy
   - human vs machine output separation
   - what hosts should rely on vs what remains internal
   - how this relates to SDK and future non-TUI hosts
4. Keep the output aligned with existing work; do not reinvent parallel abstractions if current seams are sufficient.

In scope:
- stable runtime/event/output contract direction
- relation to existing SDK and CLI/TUI surfaces

Out of scope:
- shipping a new protocol implementation in this unit

---
id: '259.2'
title: Supervise non-blocking hook tasks so detached failures are surfaced
slug: supervise-non-blocking-hook-tasks-so-detached-fail
status: closed
priority: 1
created_at: '2026-04-15T02:53:15.857199Z'
updated_at: '2026-04-15T03:35:06.894136Z'
notes: |-
  ---
  2026-04-15T03:26:04.528448+00:00
  Mid-implementation state: added `HookBackgroundEvent` plus optional `background_reporter` to `imp-core::hooks::HookRunner`, and wired `Agent::new()` to forward background hook failures into `AgentEvent::Error`. Non-blocking shell hooks are no longer blind fire-and-forget; they now run through a supervised helper that reports non-zero exits and join/panic failures. Focused tests were added for prompt return and failure reporting. Important implementation nuance discovered during verification: a real spawned-task panic in the panic-path test produces noisy panic stderr during `cargo test` even when join failure is reported correctly. Refined plan: keep the production supervision path, but verify panic/join reporting through a helper-level outcome test (or equivalent non-noisy seam) rather than triggering a real background panic in the test harness.

  ---
  2026-04-15T03:26:10.664732+00:00
  Updated next steps after test nuance: replace the current noisy `hook_non_blocking_panic_is_reported` approach with a deterministic helper-level test around `report_non_blocking_hook_outcome(...)` (or similar seam) that feeds a synthetic `JoinError`/panic-like result into the reporting path without emitting panic output to stderr. Then rerun `cargo fmt`, `cargo test -p imp-core hooks::tests`, and a narrow agent hook smoke test.

  ---
  2026-04-15T03:35:06.894120+00:00
  Implemented non-blocking hook supervision. `imp-core::hooks::HookRunner` now supports an optional `background_reporter` and emits structured `HookBackgroundEvent::{NonBlockingHookFailed,NonBlockingHookPanicked}` for non-blocking shell hooks instead of silently discarding failures. `Agent::new()` now installs a reporter that forwards these background hook failures into `AgentEvent::Error`, giving the runtime/UI an owner for detached hook failures. Added focused tests covering: non-blocking hooks still return promptly, non-blocking hook command failure is reported, and join/panic failures map to the panic event path via `report_non_blocking_hook_outcome(...)` without noisy spawned-task stderr. Verification passed: `cargo fmt --all`, `cargo test -p imp-core hooks::tests -- --nocapture`, and `cargo test -p imp-core agent::tests::agent_fires_hooks -- --nocapture`.
labels:
- cleanup
- panic
- error-handling
- hooks
parent: '259'
verify: cd /Users/asher/imp && cargo test -p imp-core hooks::tests
checkpoint: '9e044bfdfe03769494318cdd2364fd97dcf585cd'
verify_hash: cc812b4a4525fb783ea93c6a482ed560a0e9f8f7d55f3be1ec7bca7e273efb80
claimed_by: imp
claimed_at: '2026-04-15T03:19:37.586241Z'
kind: job
paths:
- /Users/asher/imp/crates/imp-core/src/hooks.rs
- /Users/asher/imp/crates/imp-core/src/agent.rs
attempt_log:
- num: 1
  outcome: abandoned
  agent: imp
  started_at: '2026-04-15T03:19:37.586241Z'
---

Implement the next cleanup pass for detached/background task failure handling in non-blocking hooks.

Goal:
- Remove the current fire-and-forget blind spot in `imp-core/src/hooks.rs` for non-blocking shell hooks.
- Keep hooks non-blocking from the caller's perspective, but ensure failures and panics have an owner and are surfaced in a structured way instead of disappearing silently.

Current state:
- Blocking hooks are awaited directly.
- Non-blocking hooks are spawned and ignored.
- If a non-blocking hook panics or fails unexpectedly, there is no durable/reportable path back to the agent/UI beyond whatever stderr may happen to do.

Work:
1. Inspect the current hook execution path in `imp-core/src/hooks.rs`, especially the branch for `blocking: false` shell hooks.
2. Introduce lightweight supervision for non-blocking hooks.
   - Keep execution asynchronous/non-blocking.
   - Capture normal completion/failure and panic/join failure.
   - Route failures into an existing structured channel if one exists, or add the narrowest new reporting path that fits current architecture.
3. Preserve current behavior for successful blocking and non-blocking hooks.
4. Add focused tests for:
   - non-blocking hook still returns promptly
   - non-blocking hook failure is observable/reported
   - panic/join failure does not disappear silently

Files:
- /Users/asher/imp/crates/imp-core/src/hooks.rs (modify — add supervision/reporting for non-blocking hooks)
- /Users/asher/imp/crates/imp-core/src/agent.rs (inspect — follow any existing tool/UI event reporting pattern if needed)
- /Users/asher/imp/crates/imp-core/src/tools.rs (inspect if tool update/reporting channel integration is needed)

In scope:
- detached task supervision for non-blocking hooks
- structured failure surfacing

Out of scope:
- provider stream task refactors
- broad hook system redesign
- changing blocking hook semantics

Verify:
- cd /Users/asher/tower && cargo test -p imp-core hooks::tests

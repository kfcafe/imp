---
id: '28.1.5'
title: Fix native imp delegate worker defaults for OpenAI OAuth/Codex and empty-string overrides
slug: fix-native-imp-delegate-worker-defaults-for-openai
status: open
priority: 1
created_at: '2026-04-14T09:32:33.136198Z'
updated_at: '2026-04-16T01:01:53.613078Z'
notes: |-
  ---
  2026-04-15T03:02:59.014511+00:00
  Smoke-tested native `imp run` on 2026-04-15 with temporary unit 49 using `../target/debug/imp ... run 49 --defer-verify`. Result: CLI path reaches startup + agent build (`agent_built`, `prompt_ready`, `run_loop_started`) but provider resolution is wrong for direct headless runs. `--provider openai -m gpt-5.4-nano` produced HTTP 401 with `Incorrect API key provided: ''`, even though `imp secrets list` shows saved OpenAI/ChatGPT OAuth entries and no `OPENAI_API_KEY` env var is set. `--provider anthropic` produced HTTP 401 `x-api-key header is required` because no API key env var was present. `--provider openai-codex -m gpt-5.3-codex` reached provider call using saved OAuth but failed with HTTP 400 `Instructions are required`. This confirms the native `imp run` path is wired and starts correctly, but direct worker auth/instruction defaults are still broken for OpenAI OAuth / Codex and empty-string API key handling.

  ---
  2026-04-15T03:03:47.835479+00:00
  Decomposed follow-up execution after the 2026-04-15 smoke test into two concrete child jobs: (1) fix direct `imp run` auth/default resolution and empty-string API key override behavior; (2) fix the OpenAI Codex/direct-worker request path so required instructions are always present. After those land, rerun the same minimal headless smoke path end-to-end as the parent acceptance check.

  ---
  2026-04-15T03:33:02.463188+00:00
  Open naming question from user: `delegate` is likely not the right verb for the imp tool. Candidate direction: preferred CLI surface `imp work <id>` for headless mana-unit execution, with the native imp tool reworded around running/spawning another imp worker rather than delegating. Treat as an unresolved vocabulary/UX decision to settle before the surface refactor lands.
labels:
- bugfix
- imp
- oauth
- openai-codex
- worker-runtime
- imp-tool
parent: '28.1'
kind: job
decisions:
- 'User preference/decision: for direct headless `imp run`, auth resolution should check for saved auth first (especially active ChatGPT/OpenAI OAuth) and only then fall back to API-key sources. Empty-string API key values must not override saved auth.'
- 'Clean auth design direction for direct `imp run`: separate (a) provider-surface selection from (b) credential resolution. Normalize empty-string overrides to `None` at the CLI/session boundary. Then resolve credentials through one shared function with precedence `explicit non-empty override -> saved auth -> stored api key -> env`, returning a typed result or a clear error rather than `""`. Do not special-case `openai` to bypass saved OAuth in worker/session code; compatibility decisions about `openai` API vs ChatGPT/Codex subscription belong in provider-surface selection, not in ad hoc auth branches.'
- 'Implementation plan direction: reduce auth complexity by separating runtime connection selection into two phases. Phase 1 selects the provider surface/model route (`openai`, `openai-codex`, etc.) based on explicit flags, model compatibility, and available saved auth. Phase 2 resolves credentials for the chosen surface through one shared path with precedence `explicit non-empty override -> saved auth -> stored api key -> env`, and returns a real credential or a clear error. Headless worker runs must not convert auth failure into an empty string token.'
- 'User concern: the current `openai` vs `openai-codex` naming is conceptually confusing because these look like providers when they are really two different OpenAI-owned execution surfaces with different auth and wire contracts. Design direction: treat these as runtime surfaces/routes rather than user-facing providers where possible, and review naming so the codebase distinguishes surface selection from credential resolution more cleanly.'
- 'User expectation: unless explicitly overridden by CLI flags or task-level assignment, direct `imp run` should inherit the same configured model/thinking defaults the user selected (e.g. `gpt-5.4` + `xhigh`). Headless worker/provider resolution should not silently route to a different surface just because auth happens to be available there without making that selection legible.'
- 'Design direction: make OpenAI routing model-first and surface-based. User-facing defaults should be expressed in terms of model + thinking (and optionally an explicit route override), not by exposing `openai-codex` as if it were a separate provider. Internally, keep the distinct transport/auth contracts, but represent them as OpenAI runtime surfaces/routes rather than duplicate pseudo-providers when refactoring the worker/runtime path.'
- 'Refactor direction after user review: stop treating `openai` and `openai-codex` as peer user-facing providers in worker/runtime selection. Preserve the distinct transports internally, but model them as OpenAI runtime surfaces/routes (API route vs ChatGPT subscription route). Keep the user-facing default mental model centered on desired model + thinking, with route selection happening underneath and made legible only when it matters.'
- 'Default inheritance rule for direct `imp run`: unless explicitly overridden by CLI flags or unit-level assignment, headless worker runs should inherit configured model/thinking defaults (for example `gpt-5.4` + `xhigh`) rather than silently switching to a different surface/provider identity because a different auth path happens to be available.'
- 'New naming candidate from user: the headless unit-worker CLI surface may be better expressed as `imp mana <id>` because the runtime is imp acting on mana-shaped work. Do not proceed into the shared runtime-connection refactor (`28.1.5.3.2`) until the CLI naming/vocabulary decision is settled.'
- 'Naming evaluation after reviewing current repo state: `imp mana <id>` is semantically attractive because it frames imp as the runtime operating on mana-shaped work, and the repo already treats the native mana surface as first-class. However, a bare `imp mana <id>` form may paint us into a corner if we later want a real `imp mana ...` namespace for multiple mana-aware CLI operations (`status`, `show`, `logs`, etc.). Provisional recommendation: prefer a namespaced form such as `imp mana work <id>` or `imp mana run <id>` for extensibility, with room for a sugar alias later if desired. Do not proceed with 28.1.5.3.2 until the top-level CLI naming is settled.'
- 'CLI naming direction is leaning toward `imp mana <id>` as the canonical single-unit worker entrypoint. Reasoning: it expresses imp as the runtime operating on mana-shaped work while remaining extensible if the `imp mana` parser treats reserved verbs (`status`, `show`, `logs`, etc.) as subcommands and otherwise interprets the first positional token as a unit id. Caveat: this stays clean as long as unit IDs do not collide with reserved verb names; current observed unit IDs are numeric/dotted, so that is acceptable for now.'
- 'Refined naming direction after user clarification: `imp mana <id>` appears extensible enough if the CLI parser reserves a small set of verbs (`status`, `show`, `logs`, etc.) under the `imp mana` namespace and otherwise interprets the first positional token as a unit id. This would let `imp mana 28.1.5.3.2` coexist cleanly with `imp mana status`. Keep this as the leading candidate while we compare ergonomics against an explicit namespaced form like `imp mana work <id>`.'
- 'CLI naming decision direction is now concrete enough for implementation planning: prefer `imp mana <unit-id>` as the canonical single-unit worker entrypoint, keep `imp run <unit-id>` as the compatibility alias during migration, and stop growing the temporary `imp work` spelling. Treat `imp mana` as a namespace with reserved operator verbs such as `status`, `show`, and `logs`, and otherwise interpret the first positional token as a mana unit id.'
- 'Architecture clarification from repo inspection: in the active Tower codebase, durable scheduling/orchestration belongs to mana itself, not to the old Pi extension. Evidence: `imp/docs/proposals/imp-run-worker-contract-and-mana-run-handoff.md` explicitly assigns ready-target selection, dependency ordering, parallel scheduling, verify batching, and durable run state to `mana run`; `imp/crates/imp-core/src/tools/mana.rs` already wraps native mana orchestration; and `mana/crates/mana-pool/src/dispatch.rs` / `mana-core/src/ops/run.rs` implement the ready-queue/dispatch engine. The Pi extension at `~/.pi/agent/extensions/mana/` is useful reference/prototype material, but it is not the active canonical scheduler for Tower.'
- 'Local implementation direction from the latest discussion: when refactoring direct worker/runtime selection around `imp mana ...`, optimize for native tool/runtime use rather than bashing the CLI from inside the agent. The eventual common path should be native mana-backed execution surfaces that feel automatic to the agent; CLI spelling changes are secondary operator UX and should not become the primary internal execution mechanism.'
- Live smoke evidence for the remaining Codex/OpenAI instructions bug must come from a freshly built `imp-cli`/`imp` binary after library changes. A stale prebuilt `../target/debug/imp` binary is not authoritative for closing 28.1.5.2, even if it still returns HTTP 400 `Instructions are required`.
---

## Goal
Make native `imp` worker delegation behave correctly when the operator or caller relies on configured defaults or OAuth-backed OpenAI auth.

## Current verified state
- A native `imp` tool delegation smoke test against imp-local mana unit `46.1` reached the worker runtime.
- Forcing `provider="openai"` with OAuth-only auth fails because the provider auto-switch to `openai-codex` only happens when provider selection is left to normal resolution.
- The `imp` tool currently treats optional string params like `system_prompt`, `model`, and `provider` as present even when they are the empty string.
- In `crates/imp-core/src/tools/imp.rs`, both unit and ad-hoc delegation paths map empty `system_prompt` to `Some("")`.
- In `crates/imp-core/src/builder.rs`, any `Some(system_prompt)` overrides assembled prompt generation entirely.
- With OpenAI OAuth / `openai-codex`, that empty override produces an empty instructions field and the provider call fails with `HTTP 400 Bad Request: {"detail":"Instructions are required"}`.

## Scope
- In scope:
  - normalize optional delegate params so empty strings are treated as absent
  - preserve auto provider resolution for OAuth-backed OpenAI/Codex flows
  - add regression coverage for the native `imp` tool delegate path and worker session creation path
- Out of scope:
  - redesigning broader auth UX
  - changing provider selection semantics outside the bug fix needed here

## Files to inspect
- `imp/crates/imp-core/src/tools/imp.rs`
- `imp/crates/imp-core/src/imp_session.rs`
- `imp/crates/imp-core/src/mana_worker.rs`
- `imp/crates/imp-core/src/builder.rs`
- `imp/crates/imp-cli/src/lib.rs`
- `imp/crates/imp-llm/src/providers/openai_codex.rs`

## Concrete tasks
1. Update native `imp` tool delegate param parsing so optional string fields ignore empty-string values instead of converting them to `Some("")`.
2. Confirm unit delegation and ad-hoc delegation both preserve default model/provider/system prompt behavior when those fields are omitted or blank.
3. Add regression coverage showing an OAuth-only OpenAI setup can still reach the ChatGPT/Codex path when provider is not forced.
4. Add regression coverage for blank `system_prompt` not clobbering the assembled prompt in delegate-driven worker runs.
5. Re-run a native `imp` delegate smoke test against a safe unit after the fix.

## Done / Verify
Done when native `imp` delegation no longer breaks OAuth-backed OpenAI runs via accidental empty-string overrides, and the behavior is covered by tests.
Verify with:
`cd /Users/asher/tower && cargo test -p imp-core --lib && cargo test -p imp-llm openai_codex -- --nocapture && cargo check -p imp-cli`

---
id: '45.4'
title: Phase 3 — introduce runner protocol and local adapter
slug: phase-3-introduce-runner-protocol-and-local-adapte
status: open
priority: 2
created_at: '2026-04-08T17:15:13.359444Z'
updated_at: '2026-04-13T07:07:04.858495Z'
acceptance: A phase epic exists with child jobs for runner protocol design, local adapter planning, and integration sequencing.
notes: |-
  ---
  2026-04-08T17:32:33.109299+00:00
  Decision from user on 2026-04-08: the first local runner implementation should start inside `imp` as a local adapter, while the protocol remains Tower-level from the start. Future jobs in this phase should therefore distinguish the shared `tower-contracts` runner protocol from the initial implementation placement inside `imp`.

  ---
  2026-04-09T04:07:12.450763+00:00
  User suspects the boundary language around `mana run` may be wrong. Preferred reframing: `imp run {mana_id}` makes clearer that the imp runtime reads mana state and performs the task, rather than mana being the primary runtime calling imp. Future runner/orchestration decisions should revisit command and ownership language from that lens.

  ---
  2026-04-09T04:47:50.132361+00:00
  Decision from user on 2026-04-09: the architecture should actively move toward `imp run {mana_id}` as the primary execution model, with `mana run` treated as legacy/compatibility behavior. Phase 3 runner/orchestration work should now be framed around imp as the orchestrator/runtime reading mana state, not mana as the primary runtime dispatch command.

  ---
  2026-04-09T05:42:40.485710+00:00
  Execution-boundary answer snapshot: imp is the orchestrator/runtime. Promote `imp run {mana_id}` as the primary execution model. Treat current mana spawning/dispatch (`mana-cli/src/spawner.rs`) and `mana run`-centered language as transitional compatibility behavior to be phased out.

  ---
  2026-04-09T05:55:01.845796+00:00
  Visible decomposition delta from `docs/rebuild/imp-run-primary-execution-path.md`: the minimum sequence for imp-run primacy is now externalized across existing root units. 45.4.3 carries the `mana run` narrowing / `spawner.rs` compatibility track; 45.3.4 carries the task-packet shift needed to demote `prompt.rs` from prompt-authoring center; 45.6.4 carries convergence of worker result semantics across inline and deferred verify; 45.5.2 carries cleanup/quarantine of stale duplicate imp-side mana loading paths; 45.9 carries the cross-track readiness checklist tying these slices together.

  ---
  2026-04-09T13:20:38.740650+00:00
  Runner protocol surface now concretized by docs/rebuild/runner-protocol-surface.md. Visible first-slice decomposition: shared Tower-level request/result groups for worktree, sandbox, command execution, capture, and cancellation; explicit binding to durable execution/worktree/sandbox lease IDs; first implementation remains an imp-local adapter rather than a root runner subtree. Follow-on unit 45.4.2 should consume this spec by mapping shared request/result groups into imp-local adapter responsibilities across agent.rs, mana_worker.rs, and tools/mana.rs. Follow-on unit 45.4.3 should use this spec to narrow spawner.rs into compatibility translation/delegation around the same typed runner boundary instead of shell-template canonical ownership.

  ---
  2026-04-13T07:07:04.835799+00:00
  2026-04-13 decomposition externalized from the current conversation: created child `45.4.4 Plan the cutover from current imp run plus mana run coexistence to a lease-based imp attach path`. This child should turn the newly recorded migration stages into a repo-grounded cutover sequence: canonical mana run/lease/event semantics first, then dual-write, then shadow scheduling/validation, then lease-based imp attach/heartbeat/checkpoint/resolve, then transcript ownership fully in imp, then `mana run` deprecation/narrowing. It must stay audit-plus-sequencing oriented and preserve `imp run {mana_id}` as the preferred live boundary.
labels:
- tower
- rebuild
- phase-3
- runner
parent: '45'
dependencies:
- '45.2'
- '45.3'
- '45.3.1'
- '45.3.2'
- '45.3.3'
kind: epic
decisions:
- 'Architectural direction settled from the repo-grounded execution-path note: treat `imp run <unit-id>` as the preferred primary execution framing for single-unit work. Treat `mana run` as legacy/compatibility orchestration behavior during migration, not as the intended long-term execution center.'
- '2026-04-13 decomposition decision: the next cross-project migration planning under Phase 3 should explicitly model the attach-path cutover instead of only talking about runner protocol and spawner demotion in the abstract. The graph now carries a dedicated child for the staged coexistence-to-attach migration so later work does not skip dual-write and shadow-validation phases.'
---

Goal: move orchestration toward a typed runner boundary for worktrees, sandboxing, command execution, and artifact capture. This phase depends on the initial contracts spine and mana durable lease/artifact planning.

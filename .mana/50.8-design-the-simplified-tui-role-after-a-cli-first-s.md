---
id: '50.8'
title: Design the simplified TUI role after a CLI-first shift
slug: design-the-simplified-tui-role-after-a-cli-first-s
status: closed
priority: 2
created_at: '2026-04-09T06:08:20.460826Z'
updated_at: '2026-04-24T05:37:29.421549Z'
notes: |-
  ---
  2026-04-09T06:10:32.326884+00:00
  Simplified-TUI design must preserve one of the strongest current values of imp-tui: pleasant editing/browsing for settings and personality. Recommendation to evaluate: keep TUI as an optional rich editor/control-panel surface for settings, personality, auth status, and inspection, even if the default authoring path moves to a CLI shell. The key shift is that these are not TUI-owned capabilities; the TUI is one adapter over shared config/auth/personality state.

  ---
  2026-04-09T06:11:34.990283+00:00
  Simplified-TUI role update: preserve one of the strongest existing imp-tui values as an optional rich control-panel/editor surface for settings, personality, auth status, and session/view inspection. The TUI may still be the nicest place to browse and edit these, but it should no longer be the only good path. Evaluate TUI as a rich adapter over shared config/auth/personality/session state rather than the owner of those capabilities.

  ---
  2026-04-09T06:27:33.682485+00:00
  Simplified-TUI role grounding from current implementation:
  - the current TUI already contains a substantial settings editor, personality editor, and welcome/setup flow. If the TUI is simplified later, do not accidentally delete these capabilities; instead separate capability ownership from fullscreen presentation.
  - likely direction: keep a richer optional control-room/editor surface for deep settings/personality/setup browsing, while CLI gets guided flows with parity for the same underlying state.

  ---
  2026-04-09T07:25:51.392110+00:00
  Command-grammar direction from docs/rebuild/imp-command-grammar.md: `imp tui` should be explicit and no longer treated as the long-term default identity of the product. The TUI remains valuable for sidebar/tool inspection, richer widgets, split-pane browsing, and focused sessions, but should be designed as a fullscreen presentation adapter over the shared runtime/surfaces rather than the canonical default entrypoint.

  ---
  2026-04-09T12:01:10.901523+00:00
  Grounded the unit in existing rebuild docs (`imp-cli-first-surface`, `imp-command-grammar`, `imp-view-submodes-and-entrypoints`, `imp-cli-shell-transcript-ux`) and scanned `imp/crates/imp-tui/src`. Key implementation grounding: current TUI already includes substantial fullscreen chat/editor + sidebar behavior plus dedicated settings, personality, welcome/setup, session picker, tree, login, secrets, model selector, and ask/command/file-finder overlays. Planning doc will separate capability ownership from fullscreen presentation and compare minimal composer vs viewer-oriented TUI roles.

  ---
  2026-04-09T12:01:50.201171+00:00
  Attempt 2 grounding update: loaded mana skill and inspected unit 50.8 directly before writing. Two related planned docs referenced by sibling units (`docs/rebuild/imp-viewer-surface.md`, `docs/rebuild/imp-tui-adapter-boundary.md`) are not present yet in repo reality, so this doc will ground itself in the docs that do exist now (`imp-cli-first-surface`, `imp-command-grammar`, `imp-cli-shell-transcript-ux`, `imp-view-submodes-and-entrypoints`) plus the current `imp-tui` code scan rather than assuming those sibling specs are already written.

  ---
  2026-04-09T12:03:05.967536+00:00
  Grounding gathered before drafting doc: current `imp-tui` is already a bundle of multiple distinct surfaces, not just chat. `app.rs` `UiMode` includes Normal, ModelSelector, CommandPalette, FileFinder, LoginPicker, SecretsPicker, TreeView, Settings, Personality, SessionPicker, and Welcome. The current TUI also has a substantial sidebar/log inspector (`views/sidebar.rs`) with stream and split list/detail modes, a status bar carrying cwd/session/model/token/cost/context/activity info (`views/status.rs`), a full settings editor (`views/settings.rs`), and a personality editor with builder/source tabs plus global/project scope (`views/personality.rs`). This supports the direction that CLI-first should shift ownership away from the fullscreen TUI without deleting these capabilities; the TUI should be treated as an adapter over shared config/auth/personality/session/tool-inspection state.

  ---
  2026-04-09T12:03:12.575969+00:00
  Completed `docs/rebuild/imp-simplified-tui-role.md` and verified the required phrases. The doc is grounded in current `imp-tui` structure from `imp/crates/imp-tui/src/app.rs` and related view modules: current main layout is top bar + widget tray + chat/messages + editor/ask area, with optional sidebar split; current fullscreen modes include settings, personality, welcome/setup, session picker, tree, login, secrets, model selector, and overlays. Recommendation captured: after the CLI-first shift, `imp tui` should primarily be a minimal composer/control-room surface with prompt box, status/widget row, active transcript, and optional sidebar; browse-heavy logs/sessions/branches/activity ownership should move to `imp view`, and routine authoring/control should move to the CLI shell. Preserved key note that settings/personality/auth/setup remain valuable in TUI as optional rich editors/control panels, but no longer TUI-owned capabilities.

  ---
  2026-04-09T12:03:49.252474+00:00
  Wrote `docs/rebuild/imp-simplified-tui-role.md` and passed the unit verify gate. Doc recommendation: keep `imp tui` as an explicit fullscreen composer/control-panel adapter over shared runtime/config/auth/personality/session state, with prompt box + compact status/widget row + optional sidebar/tool inspection. Compared that against a viewer-oriented TUI and argued browse-heavy responsibilities should move to `imp view` while default authoring and routine control move to the CLI shell.

  ---
  2026-04-09T12:04:36.067486+00:00
  Durable decomposition from the completed simplified-TUI role doc (`docs/rebuild/imp-simplified-tui-role.md`): recommended target is a composer-first fullscreen adapter, not a viewer-first surface. Preserve `imp tui` around (1) prompt box / current-session composition, (2) compact status/widget row, (3) optional sidebar/tool inspection, and (4) optional rich control-panel editing for settings/personality/auth/setup/session detail. Do not preserve the current heavy fullscreen layout by default just because it exists today; preserve the role and strengths instead.

  ---
  2026-04-09T12:05:10.599266+00:00
  Completed `docs/rebuild/imp-simplified-tui-role.md` and verified with the unit gate. Doc grounding cites current `imp-tui` structure (`app.rs` UiMode breadth, `views/sidebar.rs`, `views/status.rs`, `views/settings.rs`, `views/personality.rs`) plus adjacent CLI-first/view docs. Recommendation recorded: after the CLI-first shift, `imp tui` should default toward a minimal composer/control-room role (prompt box + compact status/widget row + optional sidebar + rich settings/personality/auth/setup editing), while the viewer-oriented role for logs/sessions/branches/agent activity should be owned by `imp view`. Explicitly states that settings/personality/auth/setup remain valuable in the TUI but should become shared capabilities rather than TUI-owned ones.

  ---
  2026-04-09T12:06:12.267142+00:00
  Durable decomposition externalized from the completed spec so follow-on work can inherit it cold: the recommended split is not 'choose minimal composer OR viewer-oriented TUI' in one command. Instead: (1) `imp tui` should default toward the minimal composer/control-room role — prompt/composer surface + compact status/widget row + optional sidebar + focused current-session inspection + optional rich editors for settings/personality/auth/setup; (2) `imp view` should own the viewer-oriented role — logs, sessions, branches/tree, checkpoints, and agent-activity browsing; (3) config/auth/personality/setup/session capabilities should be shared underlying surfaces consumed by shell, viewer, and TUI rather than owned by fullscreen presentation. Product consequence to preserve: do not let `imp tui` regress into the canonical browse-heavy surface or the only good path for settings/personality/auth/setup.

  ---
  2026-04-09T15:05:00.716447+00:00
  Cross-unit durable delta from closed sibling 50.4 (`docs/rebuild/imp-tui-adapter-boundary.md`) so simplified-TUI work inherits the boundary cold: the recommended composer/control-room role for `imp tui` now has an explicit ownership seam. TUI-local scope is fullscreen presentation state only (rendering/layout, panes/sidebar, overlays, selection, keyboard/mouse navigation, focused current-session transcript/composer display, tool-output inspection, rich fullscreen editor UX). Shared runtime/session/config/auth/personality/setup surfaces remain upstream. Browse-heavy cross-session inspection should continue to bias toward `imp view` rather than regrowing inside `imp tui` by default.

  ---
  2026-04-09T15:05:49.525171+00:00
  Cross-unit boundary delta from completed 50.4: the simplified TUI role now has an explicit ownership seam. Future `imp tui` work should stay within TUI-local presentation state only: rendering/layout, panes/sidebar behavior, overlays, selection, keyboard/mouse navigation, focused current-session transcript/composer display, status/widget presentation, and tool-output inspection. Do not let simplified-TUI work drift back into owning runtime bootstrap, model/provider/auth/config resolution, session semantics, or browse-heavy cross-session browsing; those remain upstream/shared or bias toward `imp view`.

  ---
  2026-04-24T05:37:29.421547+00:00
  Graph hygiene pass 7 2026-04-24: verify passed for simplified TUI role doc. Closing stale in_progress design unit.
labels:
- architecture
- imp
- tui
- ux
parent: '50'
verify: cd /Users/asher/imp && test -f docs/rebuild/imp-simplified-tui-role.md && rg -q 'prompt box' docs/rebuild/imp-simplified-tui-role.md && rg -q 'status/widget' docs/rebuild/imp-simplified-tui-role.md && rg -q 'sidebar' docs/rebuild/imp-simplified-tui-role.md && rg -q 'viewer-oriented' docs/rebuild/imp-simplified-tui-role.md && rg -q 'CLI-first' docs/rebuild/imp-simplified-tui-role.md
checkpoint: '3db48f3cb25ee6a979ff4a692e0056be7278e677'
verify_hash: baaa2e7bb18c6f85856c8660a508649719f663dfa4c92b0a6a74d2276e011cd5
kind: epic
attempt_log:
- num: 1
  outcome: abandoned
  agent: imp
  started_at: '2026-04-09T12:00:58.711663Z'
  finished_at: '2026-04-24T05:37:11.397882Z'
---

Write `docs/rebuild/imp-simplified-tui-role.md` defining what a simplified TUI should be if imp becomes CLI-first. Ground it in current imp-tui structure plus the latest user ideas. Compare two target roles: (1) a minimal composer surface with prompt box, status/widget row, and optional sidebar; (2) a viewer-oriented TUI focused on logs, sessions, branches, and agent activity. Explain which responsibilities should leave the TUI and move to the CLI shell or viewer surface. Preserve the idea that sidebar/tool inspection is valuable, but do not assume the current heavy fullscreen layout must remain. This unit is about ownership and product role, not visual polish or implementation.

---
id: '50.9'
title: Implement the first CLI-first proving slice with explicit chat/view/tui entrypoints and minimal shell commands
slug: implement-the-first-cli-first-proving-slice-with-e
status: open
priority: 1
created_at: '2026-04-09T07:05:43.744732Z'
updated_at: '2026-04-24T05:36:58.533811Z'
notes: |-
  ---
  2026-04-09T07:08:51.734317+00:00
  Execution update per direct user instruction on 2026-04-09: begin by adding explicit `imp chat` and the first CLI-shell implementation surfaces now, then continue refining boundaries from the concrete work. This temporarily supersedes the earlier sequencing assumption that 50.6.5 and 50.7.1 must fully land first. Keep the implementation slice narrow and reversible: explicit `imp chat`, explicit `imp tui` alias/entrypoint preservation, and the first minimal shell command subset. Do not wait on complete boundary closure before starting the proving slice.

  ---
  2026-04-09T07:22:40.881035+00:00
  Durable constraint from 50.1 spec for the proving slice: implementation should follow the architecture-first first slice rather than behaving like a TUI rewrite. Keep the proving slice narrow and reversible; preserve the current fullscreen path; make explicit chat/view/tui surfaces visible without deleting current behavior; and only extract enough shared runtime/startup structure to support the CLI-first model honestly. The proving slice should not prematurely settle every command-taxonomy choice or depend on the final default-entrypoint decision landing first.

  ---
  2026-04-09T07:25:28.600255+00:00
  Implementation contract from docs/rebuild/imp-command-grammar.md: the first proving slice should expose explicit `imp chat`, `imp view`, and `imp tui` entrypoints and add `imp prompt` as the canonical named one-shot path while preserving current `-p`/`--print` behavior. During the proving slice, do not flip plain `imp` to chat yet unless the CLI shell already covers the minimum daily workflow. Preserve compatibility aliases (slash commands, `web-login`) and keep worker semantics crisp: `imp run <unit-id>` remains separate from authoring/chat behavior.

  ---
  2026-04-09T07:27:40.079760+00:00
  Implementation progress on 2026-04-09:
  - Added explicit top-level subcommands in `imp-cli`: `chat`, `tui`, and placeholder `view {area?}`.
  - `chat` now runs a new line-oriented CLI shell path in `imp/crates/imp-cli/src/main.rs`.
  - Current shell implementation includes:
    - status line showing project/model/thinking/session
    - `imp>` prompt
    - command parsing for both `:command` and compatibility `/command`
    - first minimal command subset: `help`, `quit`, `model`, `thinking`
    - transcript-oriented agent run output with compact tool notices, thinking indicator, and summary line
    - CLI `UserInterface` adapter for confirm/select/input so ask-style interactions can work in plain terminal flows
  - `tui` explicitly launches the existing fullscreen interface.
  - `view` currently prints a clear placeholder message and points users to `imp tui` / `imp chat` until viewer surfaces land.
  - Updated `imp/README.md` quick-start/TUI section to mention `imp chat`, `imp tui`, and planned `imp view`.

  Verification evidence so far:
  - `cargo check -p imp-cli` passed.
  - targeted tests passed:
    - `cargo test -p imp-cli parse_chat_shell_command -- --nocapture`
    - `cargo test -p imp-cli shell_session_choice -- --nocapture`
    - `cargo test -p imp-cli mana_unit_task_prompt -- --nocapture`
  - manual smoke run passed for shell command loop:
    - `printf ':help\n:model sonnet\n:thinking high\n:quit\n' | cargo run -q -p imp-cli -- --no-tools chat`

  Noted follow-up from compilation:
  - existing mana-unit loading/task-prompt code in `imp-cli/src/main.rs` is now dead-code from the compiler's perspective in the current proving slice, producing warnings but not blocking build/check.
  - `imp view` remains placeholder-only in this slice; browsing-heavy surfaces are still pending under the viewer units.
  - shell currently supports single-line prompts only; multiline compose, richer command set, and guided parity flows remain future work.

  ---
  2026-04-09T07:28:49.119550+00:00
  Durable follow-up decomposition after landing the first `imp chat` proving slice:

  What is now proven:
  - explicit `imp chat` exists as a line-oriented shell
  - explicit `imp tui` exists as the fullscreen path
  - explicit `imp view` exists as a placeholder entrypoint
  - the shell currently supports the minimal command subset (`:help`, `:quit`, `:model`, `:thinking`) plus transcript-oriented output and a plain-terminal `UserInterface` adapter

  Recommended next implementation sequence from this point:
  1. extend `imp chat` just enough to feel operationally useful without bloating it:
     - `:status`
     - `:new`
     - `:resume`
     - `:compact`
     - keep shell transcript-oriented and single-pane
  2. make `imp view` real for the first focused browsing-heavy tasks:
     - likely start with `sessions`, `tree`, and `logs`
     - keep handoff explicit from the shell
  3. after shell/view proving slices are stable, implement guided CLI parity flows:
     - `imp settings`
     - `imp personality`
     - `imp setup`

  Execution rule to preserve:
  - keep the proving slices small and reversible
  - do not let shell command growth accidentally turn `imp chat` into a full browser
  - use `imp view` as the home for browse/filter/inspect workflows as soon as practical

  ---
  2026-04-09T07:54:00.534540+00:00
  Execution sequencing pinned by 50.6.5 / `docs/rebuild/imp-default-entrypoint-migration.md`: the proving slice must preserve plain `imp` compatibility while introducing explicit `imp chat`, `imp prompt`, `imp view`, and `imp tui`. Do not flip plain `imp` during the first proving slice. The flip is deferred until the shell satisfies the documented success criteria: streaming, session resume/new, model/thinking control, compact tool notices, turn summaries, handoff to `imp view`, and escape to `imp tui`.

  ---
  2026-04-09T07:54:48.089809+00:00
  Migration-execution constraint inherited from 50.6.5: the first proving slice must NOT flip plain `imp` yet. Its job is to make the staged migration real by shipping explicit `imp chat` / `imp view` / `imp tui` entrypoints plus the minimum shell behaviors needed to prove readiness. Preserve compatibility during this slice: plain `imp` remains temporarily on the old fullscreen default, `imp -p` / `--print` remain aliases, slash commands remain compatibility aliases in the shell, and `imp web-login` can remain temporarily. The proving slice should leave the graph and docs ready for a later dedicated default-flip unit rather than quietly changing plain `imp` early.

  ---
  2026-04-09T07:55:02.414113+00:00
  Implementation decomposition to follow the command taxonomy spec in docs/rebuild/imp-cli-command-taxonomy.md: proving-slice chat work should treat `:command` as canonical, keep `/command` only as temporary compatibility, prioritize shell-first quick controls, and avoid reintroducing fullscreen-only dependencies for core operator actions. When adding direct CLI peers, follow the taxonomy families (`imp settings`, `imp personality`, `imp sessions`, `imp checkpoints`, `imp memory`, `imp ext`).

  ---
  2026-04-09T07:55:11.547036+00:00
  Visible planning delta from the resolved default-entrypoint migration policy: implementation sequencing should be interpreted as Phase 1 / Phase 2 work, not an early default flip. The proving slice and immediate follow-ons should make explicit `imp chat`, `imp prompt`, `imp view`, and `imp tui` feel real while preserving plain `imp` compatibility. Later shell/view/parity work is what earns the default flip; do not treat existence of `imp chat` alone as sufficient.
labels:
- imp
- cli
- implementation
- proving-slice
parent: '50'
kind: epic
attempt_log:
- num: 1
  outcome: abandoned
  agent: imp
  started_at: '2026-04-09T07:08:51.712Z'
  finished_at: '2026-04-24T05:36:58.533811Z'
---

Implement the first CLI-first proving slice for imp after the entrypoint and viewer decisions are explicit enough. Current planning context: 50.6.5 owns the `imp` vs `imp chat` migration decision; 50.7.1 owns the `imp view` surface; 50.6.2/50.6.3 define the command taxonomy and transcript UX. Scope this unit narrowly so it proves the direction without deleting the current TUI. Likely implementation slice:
1. add explicit top-level entrypoints for `imp chat`, `imp view`, and `imp tui`
2. preserve the current fullscreen path during migration while making the new shell/view paths explicit
3. implement the first minimal CLI-shell command subset only: `:help`, `:quit`, `:model`, `:thinking`
4. keep output transcript-oriented with compact status/tool notices rather than trying to port the whole TUI
5. avoid broad settings/personality/setup work in this slice

Relevant files likely include:
- `imp/crates/imp-cli/src/main.rs`
- new or extracted CLI-shell runtime/adapter files under `imp-cli`
- possibly shared startup/runtime seams identified by 50.3
- docs/README follow-up only if needed for the proving slice

Out of scope:
- guided settings/personality/setup parity
- full session/tree/log viewer parity
- deleting or heavily rewriting the existing TUI

This unit should stay small and reversible.

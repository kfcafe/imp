---
id: '28.1.5.1'
title: Fix direct imp run provider auth resolution and ignore empty-string API key overrides
slug: fix-direct-imp-run-provider-auth-resolution-and-ig
status: in_progress
priority: 1
created_at: '2026-04-15T03:03:47.799876Z'
updated_at: '2026-04-15T03:27:06.727861Z'
acceptance: Direct headless `imp run` no longer forwards `api_key=''` for OpenAI-style runs, saved auth/default resolution is preserved when no real API key override is provided, and focused tests cover the regression.
notes: |-
  ---
  2026-04-15T03:14:52.488371+00:00
  Inspected auth resolution. Main findings:
  1. `crates/imp-core/src/imp_session.rs` special-cases `openai` in `resolve_api_key()` to `auth_store.resolve_api_key_only("openai")`, which bypasses saved OpenAI/ChatGPT OAuth even though `AuthStore::resolve("openai")` would prefer stored OAuth before api-key/env sources.
  2. In `ImpSession::create()`, when `options.no_tools` is true, auth resolution errors are swallowed via `resolve_api_key(...).await.unwrap_or_default()`. For headless `imp run` smoke tests this becomes an empty bearer token and matches the observed HTTP 401 `Incorrect API key provided: ''`.
  3. `AuthStore::set_runtime_key()` stores whatever string it receives; there is currently no trimming/empty-string guard. That is a real secondary footgun, but it was not required to explain the observed smoke-test failure because `cli.api_key` was unset.
  4. `AuthStore::resolve()` already implements the user's preferred precedence (`runtime override -> stored OAuth -> stored/api-key field -> env`). The bug is that `imp_session` / direct worker code bypasses that path for `openai`.
  5. There is also a consistency gap between CLI fallback logic (`should_use_chatgpt_provider` only auto-switches when `cli.provider` is unset) and session fallback logic (`should_use_codex` allows explicit `provider=openai` to fall back for codex-supported models). Worth reviewing after the primary auth fix, though not the direct cause of the nano smoke-test failure.

  ---
  2026-04-15T03:18:44.929764+00:00
  Refined execution plan after auth inspection:
  1. Normalize API key overrides at the boundary (`Cli.api_key` / `SessionOptions.api_key`) so empty or whitespace-only values become `None` before any provider/auth logic.
  2. Extract or consolidate a shared runtime-connection helper used by direct headless `imp run` / `ImpSession::create` so auth resolution does not fork across surfaces.
  3. Remove the current `openai => resolve_api_key_only("openai")` worker/session special-case and instead resolve credentials for the already-selected provider surface using the shared precedence `explicit non-empty override -> saved auth -> stored api key -> env`.
  4. Remove the `unwrap_or_default()` auth fallback in no-tools/headless worker startup so auth failure stays explicit instead of becoming `api_key=""`.
  5. Add focused tests around empty-string normalization, OpenAI/ChatGPT saved-auth preference, and headless-session auth failure behavior.
  6. Re-run the minimal direct `imp run` smoke path against ChatGPT-backed OpenAI settings.

  ---
  2026-04-15T03:26:07.318560+00:00
  Broader cleanup direction after user feedback: do not optimize for the smallest patch if it preserves confusing architecture. This job can absorb a cleaner refactor that (a) separates model/default inheritance from route/auth selection, (b) makes `imp run` inherit CLI/unit/config defaults predictably, and (c) removes misleading `openai` vs `openai-codex` pseudo-provider branching from user-facing runtime logic where feasible.

  ---
  2026-04-15T03:27:06.727858+00:00
  Expanded implementation shape for clean refactor:
  1. Introduce a shared runtime-connection resolver for headless worker/session startup.
  2. Split concerns into (a) desired model/thinking/default inheritance, (b) OpenAI route selection, and (c) credential resolution.
  3. Normalize empty-string API key overrides to `None` at the boundary.
  4. Remove worker/session branches that treat plain `openai` as API-key-only and instead resolve credentials for the chosen route with precedence `explicit non-empty override -> saved auth -> stored api key -> env`.
  5. Remove auth-failure-to-empty-string fallback from no-tools/headless worker startup.
  6. Keep transport-specific request builders separate (`openai` API vs ChatGPT/Codex backend), but push that distinction below the runtime connection resolver so the top-level worker path stays model-first and legible.
  7. Add focused tests covering default inheritance, route selection, saved-auth preference, and empty-string normalization.
labels:
- bugfix
- imp
- oauth
- openai
- worker-runtime
parent: '28.1.5'
verify: cd /Users/asher/tower/imp && cargo test -p imp-cli run_headless -- --nocapture && cargo test -p imp-core auth -- --nocapture
checkpoint: '9e044bfdfe03769494318cdd2364fd97dcf585cd'
verify_hash: b8181c1f15acb4c73ae8e5dc204f73695d6108c59b0afdbacf1d617c3d50bf06
claimed_by: imp
claimed_at: '2026-04-15T03:12:43.361927Z'
kind: job
attempt_log:
- num: 1
  outcome: abandoned
  agent: imp
  started_at: '2026-04-15T03:12:43.361927Z'
---

Goal: make direct headless `imp run` resolve provider auth correctly instead of sending an empty API key string.

Current state:
- A smoke test on 2026-04-15 confirmed `../target/debug/imp ... run 49 --defer-verify` reaches `agent_built`, `prompt_ready`, and `run_loop_started`.
- `--provider openai -m gpt-5.4-nano` then fails with HTTP 401 `Incorrect API key provided: ''`.
- `imp secrets list` shows saved OpenAI/ChatGPT OAuth entries, and there is no `OPENAI_API_KEY` env var in the shell.
- This strongly suggests direct headless runs are forwarding an empty-string API key override or otherwise bypassing saved auth/default provider resolution.

Steps:
1. Inspect the direct headless worker path in `crates/imp-cli/src/lib.rs` (`run_headless_mode`) and the session/provider auth resolution path used by `WorkerRunOptions` / `ImpSession::create`.
2. Find where an empty string can override normal auth resolution for direct `imp run`.
3. Change the path so empty-string API key values are treated as absent and saved auth / provider-specific resolution still works.
4. Add focused tests for the affected resolution path so future headless worker runs do not regress.
5. Re-run a minimal direct `imp run` smoke test with OpenAI provider settings to confirm the auth path no longer emits `Incorrect API key provided: ''`.

Files:
- `crates/imp-cli/src/lib.rs` (modify — direct `imp run` setup / option plumbing)
- `crates/imp-core/src/imp_session.rs` (inspect/modify — session auth resolution path)
- `crates/imp-llm/src/auth.rs` (inspect — empty-string and saved-auth behavior)
- any focused tests near those paths

In scope:
- direct headless `imp run` auth/default resolution
- empty-string override handling
- focused tests and smoke verification

Out of scope:
- broad auth redesign
- provider-specific request-shape changes unless required for this bug

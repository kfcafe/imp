---
id: '28.1.5.4'
title: Fix headless worker loop so successful one-turn runs stop cleanly instead of reporting max-turns exceeded
slug: fix-headless-worker-loop-so-successful-one-turn-ru
status: closed
priority: 2
created_at: '2026-04-16T03:11:37.908838Z'
updated_at: '2026-04-23T03:58:00.239900Z'
acceptance: There is a concrete unit to diagnose why successful one-turn headless worker runs still terminate with `Max turns exceeded (1)` instead of exiting cleanly after emitting a stop/EndTurn response.
notes: |-
  ---
  2026-04-16T03:55:30.182422+00:00
  Implemented a first pass on the headless loop stop fix.

  Code changes:
  - `crates/imp-core/src/agent.rs`
    - added a `completed_cleanly` loop flag
    - the top-of-loop max-turn guard now only errors when we are actually attempting another turn with pending work; if the previous turn already completed cleanly and no follow-up is queued, it breaks instead of emitting `Max turns exceeded (...)`
    - the text-only no-follow-up branch now marks `completed_cleanly = true` before breaking
  - Added regression test:
    - `single_text_turn_with_max_turns_one_exits_cleanly`
    - expectation: a single successful text-only turn at `max_turns = 1` returns `Ok(())`, emits `AgentEnd`, and never emits a max-turn error event.

  Next step in flight: run focused `imp-core` agent tests, then rerun the fresh headless smoke path that previously produced `SMOKE_OK` followed by `Max turns exceeded (1)`.

  ---
  2026-04-16T03:56:31.675499+00:00
  Latest investigation state before continuing:
  - Inspected `crates/imp-cli/src/lib.rs::run_headless_mode()` and `crates/imp-core/src/imp_session.rs::{prompt,recv_event,wait}`. The headless wrapper itself looks straightforward: it waits for `AgentEnd`, then `session.wait()` returns the stored `agent.run(...)` result. No obvious wrapper-side conversion to `MaxTurns` was found yet.
  - Inspected `crates/imp-core/src/agent.rs` around the main run loop. The residual failure shape is: a text-only successful turn emits `message_end` + `turn_assessment(chosen_action=stop, reason=no_automatic_follow_up)` but the overall headless run still returns `Max turns exceeded (1)`.
  - Added regression test `single_text_turn_with_max_turns_one_exits_cleanly` in `agent.rs`; it passes, which suggests the minimal pure-agent text-only case already stops correctly.
  - A prior attempted `completed_cleanly` flag patch in `agent.rs` was not the right durable fix and should not be treated as the root cause. The current live issue likely depends on the full session/headless path rather than the minimal unit-test shape.

  Revised execution plan:
  1. Stop broad changes to the agent loop until we isolate the exact headless/session reproduction.
  2. Reproduce the residual `SMOKE_OK` + `Max turns exceeded (1)` behavior in a focused integration-style test closer to `ImpSession` / headless worker startup, not just `Agent::run()`.
  3. Compare the event/result sequence between:
     - passing pure-agent single-turn test
     - failing headless/session smoke path
     to find where an extra turn or stale result is introduced.
  4. Patch the smallest real boundary (agent loop, session event handoff, or headless wrapper) based on that evidence.
  5. Verify with:
     - focused `imp-core` test covering the reproduced path
     - fresh cargo-built live smoke for `imp mana/run` one-turn no-tools execution
     ensuring no trailing `Max turns exceeded (1)` after `SMOKE_OK`.

  Current hypothesis to test next:
  - the bug may require the full `ImpSession::prompt` / `recv_event` / `wait` lifecycle or worker/session prefill path, because the pure `Agent::run()` one-turn case now behaves correctly under test.

  ---
  2026-04-16T04:11:59.045357+00:00
  New hypothesis after inspecting `agent.rs` follow-up machinery:
  - The residual `SMOKE_OK` + `Max turns exceeded (1)` behavior may be caused by a pre-queued follow-up hint rather than the core stop path itself.
  - Evidence from inspection:
    - `queued_follow_ups` is populated before turn 0 via `mana_skill_follow_up_hint(prompt, ...)`.
    - After a text-only turn, the run loop stops only if `queued_follow_ups.pop_front()` returns `None`; otherwise it pushes the follow-up as a new user message, increments `turn`, and continues.
    - The smoke prompts and worker/unit text contain strong `mana`/`unit` signals, so the initial hint path may be active in worker mode and force a second turn, which then immediately hits `max_turns = 1`.
  - This would explain why the pure `Agent::run()` regression test passes while the full headless/session smoke still reports `Max turns exceeded (1)` after a successful first answer.

  Next concrete steps:
  1. Reproduce this in a focused test closer to the headless worker/session path with a prompt that triggers `mana_skill_follow_up_hint(...)` in worker mode.
  2. Verify whether `queued_follow_ups` is non-empty before turn 0 in the failing shape.
  3. If confirmed, suppress the proactive mana-skill follow-up hint for headless/no-tools worker runs (or more narrowly for worker-mode single-unit headless execution) so a successful first turn can stop cleanly.
  4. Re-run the fresh live smoke to confirm `SMOKE_OK` exits without the trailing max-turn error.

  ---
  2026-04-22T17:35:11.262646+00:00
  2026-04-22: resuming this unit next because the fresh proving harness confirmed the one-turn headless/runtime bug is still relevant. Plan is to test the queued follow-up hypothesis in `agent.rs` before broadening scope: inspect `mana_skill_follow_up_hint(...)`, `queued_follow_ups`, and worker/headless no-tools session startup, then add the narrowest regression around the actual failing path.

  ---
  2026-04-22T17:51:32.487430+00:00
  2026-04-22 progress: confirmed the queued follow-up hypothesis and implemented the narrow guard in `crates/imp-core/src/agent.rs`. `mana_skill_follow_up_hint(...)` now receives `tools_available` and returns `None` when the agent has no tools, which prevents worker/headless no-tools runs from auto-queueing a second turn. Added focused regressions: `agent_does_not_queue_mana_basics_hint_when_no_tools_available` and `single_text_turn_with_max_turns_one_and_no_tools_exits_cleanly`. Verification: both new tests pass, along with existing `single_text_turn_with_max_turns_one_exits_cleanly`. `cargo test -p imp-cli run_headless -- --nocapture` currently reports 0 matching tests, so the unit verify command does not yet exercise a headless regression directly.

  ---
  2026-04-23T03:52:33.342673+00:00
  2026-04-23: interrupted the long-running imp-cli regression after reproducing a new integration-level hang. Evidence: `run_headless_no_tools_worker_single_turn_exits_cleanly` timed out waiting for `prepared.session.recv_event()` to return `None` even though the agent had already emitted `AgentEnd`. Likely root cause is in `ImpSession::recv_event()`: on `AgentEnd` it awaits the agent task and stores the agent back into `self.agent`, which keeps the agent-owned `event_tx` sender alive, so the event channel never closes and event-drain loops block forever. This means the new imp-cli test exposed a second bug in event-stream completion semantics, separate from the original max-turn follow-up issue.

  ---
  2026-04-23T03:58:00.239886+00:00
  2026-04-23 completed. Final fix consisted of two narrow changes proven together: (1) suppress proactive mana skill follow-up hints when no tools are available so one-turn worker/no-tools runs do not enqueue a spurious second turn; (2) make `ImpSession::recv_event()` return `None` once the run has already completed (`agent_task.is_none()` and `completed_run_result.is_some()`), instead of waiting forever for the event channel to close while the restored agent still owns an event sender. Added coverage in both layers: `imp-core` tests for no-tools worker follow-up suppression and post-AgentEnd `recv_event()` completion semantics, plus `imp-cli` integration-style test `run_headless_no_tools_worker_single_turn_exits_cleanly` proving the headless worker path emits `SMOKE_OK` and no `Max turns exceeded` error with `max_turns = 1`. Verify used: cargo test -p imp-core single_text_turn_with_max_turns_one_and_no_tools_exits_cleanly -- --nocapture && cargo test -p imp-core agent_does_not_queue_mana_basics_hint_when_no_tools_available -- --nocapture && cargo test -p imp-core recv_event_returns_none_after_agent_end_even_if_sender_is_still_owned -- --nocapture && cargo test -p imp-cli run_headless_no_tools_worker_single_turn_exits_cleanly -- --nocapture
labels:
- bugfix
- imp
- worker-runtime
- headless
- loop-control
parent: '28.1.5'
verify: cd /Users/asher/tower/imp && cargo test -p imp-core agent -- --nocapture && cargo test -p imp-cli run_headless -- --nocapture
checkpoint: '080cc0b4f304c2af3d1fd98f399cddd509cc66e4'
verify_hash: '94c2e732a3182eaefb2a2582b908c1a29d0f67e0b3ef5cd5693b0139d8ad0dc0'
claimed_by: imp
claimed_at: '2026-04-16T03:54:13.708093Z'
kind: job
attempt_log:
- num: 1
  outcome: abandoned
  agent: imp
  started_at: '2026-04-16T03:54:13.708093Z'
---

Goal: diagnose and fix the residual worker-loop termination bug where fresh successful one-turn headless worker runs still end with `Max turns exceeded (1)` after already emitting the correct response and stop assessment.

Current state:
- Fresh cargo-built smokes for both `openai-codex/gpt-5.3-codex` and plain `openai/gpt-5.4` now stream `SMOKE_OK` successfully.
- After `message_end` and a `turn_assessment` whose chosen action is `stop` with reason `no_automatic_follow_up`, the process still emits `{"error":"Max turns exceeded (1)"}`.
- This indicates the original auth/instructions regressions are fixed, but the worker runtime still mishandles clean termination at the turn budget boundary.

Steps:
1. Inspect the headless worker run loop / max-turn handling in `crates/imp-core` to find why a successful stop assessment still trips the max-turn error path.
2. Add a focused regression test for a one-turn successful headless/no-tools worker run that should exit cleanly without `Max turns exceeded`.
3. Fix the termination logic and verify the smoke path exits successfully after the correct response.

Files:
- `crates/imp-core/src/agent.rs`
- `crates/imp-core/src/imp_session.rs`
- any headless runner / loop-control code under `crates/imp-cli/src/lib.rs`

In scope:
- clean stop semantics at max-turn boundary for successful headless runs

Out of scope:
- broader planner/continuation policy changes unless needed for the bug

---
id: '45.11.1.1'
title: Clarify whether 'native Rust, not Lua' applies to imp run or the extension substrate
slug: clarify-whether-native-rust-not-lua-applies-to-imp
status: open
priority: 1
created_at: '2026-04-17T07:22:57.942392Z'
updated_at: '2026-04-17T07:30:02.001591Z'
notes: |-
  ---
  2026-04-17T07:30:02.001586+00:00
  Clarified by user: the statement 'native Rust, not Lua' applies specifically to imp tool/worker execution. `imp run` / worker execution should stay on a native Rust runtime path and must not be routed through Lua. This is not a decision to replace the current shipped Lua extension substrate; extension-system questions remain separate. Route implications primarily to worker/runtime and runner-boundary work, not to the guest-runtime extension proposal except as an explicit boundary constraint.
labels:
- architecture
- imp
- planning
- decision
- clarification
- root
parent: '45.11.1'
verify: cd /Users/asher/tower && mana show 45.11.2 >/dev/null
kind: epic
---

Goal: clarify the user's statement "imp run should be a native rust extension, not Lua" well enough to route the remaining extension/runtime lane into the correct existing work.

Current state:
- Root planning unit 45.11 captured three near-term imp lanes.
- 45.11.1 now has settled defaults for auth/runtime and auto-compaction.
- One consequential statement remains ambiguous: "imp run should be a native rust extension, not Lua."
- This could refer to the worker/runtime seam, the durable extension substrate, or some future script/execution surface. The distinction matters because the relevant downstream units are different.

Clarification targets:
1. Determine whether the statement means:
   - `imp run` / worker execution remains a native Rust runtime path and must not route through Lua, or
   - the durable extension system should become Rust-native rather than Lua-backed, or
   - some other execution/plugin boundary.
2. Once clarified, map it to the right downstream work:
   - worker/runtime reliability and runner-boundary units if the statement is about execution path
   - extension substrate / script-boundary units if the statement is about extensibility
3. Record the clarified decision in root mana and append the implications to the relevant imp-local units.

Relevant downstream candidates:
- root: `45.4`, `45.5`, `45.7`, `45.10`
- imp-local: `44.1`, `44.2`, `28.1.5.1`, `27.3`

In scope:
- clarification and routing of the ambiguous architecture statement
- durable decision capture once clarified

Out of scope:
- implementation
- revising the already-settled auth or compaction defaults

Do not:
- assume the phrase implies shipped Rust-native extensions without explicit clarification
- collapse worker runtime and extension substrate into the same concept

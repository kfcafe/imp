---
id: '50.10.2'
title: Implement terminal-native imp personality flow for CLI parity
slug: implement-terminal-native-imp-personality-flow-for
status: open
priority: 2
created_at: '2026-04-09T07:46:41.880111Z'
updated_at: '2026-04-24T05:37:11.281426Z'
notes: |-
  ---
  2026-04-09T07:55:12.795345+00:00
  Execution contract from 50.6.4 spec:
  - Implement terminal-native personality parity around current PersonalityState behavior.
  - Preserve both builder and source modes as first-class flows.
  - Preserve explicit global vs project scope handling, show resolved current_path, and keep unsaved state per scope while switching scopes within a session.
  - Builder flow must expose Autonomy/Brevity/Caution/Warmth/Planning with the same preset buckets plus edited/missing states.
  - If a builder change would replace a manually edited source tunable, require an overwrite confirmation with diff preview before mutating source.
  - Source flow must work in plain terminal and may optionally hand off to $EDITOR or fullscreen TUI, but those are optional. Saving writes only the active scope's soul.md and updates the live shell session when invoked from `:personality`.

  ---
  2026-04-09T08:38:22.067718+00:00
  Starting implementation. Plan: add the first terminal-native personality flow to `imp-cli`, reachable as top-level `imp personality` and from `imp chat` via `:personality`. Keep the first slice real but narrow: preserve the important structure already captured from the TUI (`builder` vs `source`, `global` vs `project` scope), then verify the flow at crate scope and with shell-local smoke runs.

  ---
  2026-04-09T08:39:02.567477+00:00
  Implementation refinement after inspecting current personality code: the first CLI personality slice should mirror the real current structure rather than inventing a new config model. Grounding to preserve: `imp-core::personality` already provides `default_soul_markdown`, `generated_tunable_line`, `replace_tunable_line`, `tunable_state_for_label`, and `soul_identity_text`; `imp-core::resources` provides project soul discovery and suggested project soul path; current TUI save logic writes markdown directly to either global `<user_config_dir>/soul.md` or project `.imp/soul.md`. Likely first slice: builder-mode editor for the five tunables + explicit scope switch, plus a simple source-mode path that can print/edit the current markdown text or open the file path. Keep it terminal-native and file-backed.

  ---
  2026-04-09T08:42:24.568435+00:00
  Verification finding: crate checks/tests are passing for the new personality flow, but full binary smoke runs still exit early on provider auth before reaching the `personality` subcommand path. This suggests a dispatch-order/runtime issue in `imp-cli` main command handling rather than a personality-flow logic error. Immediate next action: inspect `main()` dispatch ordering and fix it so `imp personality` and related local command surfaces can bypass remote auth just like other local CLI-only commands.

  ---
  2026-04-09T08:43:27.833214+00:00
  Implementation progress on 2026-04-09:
  - Added top-level `imp personality` subcommand in `imp/crates/imp-cli/src/main.rs`.
  - Added `:personality` / `/personality` shell command inside `imp chat`.
  - Implemented the first terminal-native personality flow using the real file-backed soul model already used by the current TUI.

  Current first-slice behavior:
  - preserves explicit scope handling:
    - global → `<user_config_dir>/soul.md`
    - project → nearest/suggested `.imp/soul.md`
  - preserves explicit mode structure:
    - builder mode
    - source mode
  - builder mode exposes the five existing tunables via markdown-backed cycling:
    - Autonomy
    - Brevity
    - Caution
    - Warmth
    - Planning
  - source mode shows current path + identity sentence + markdown preview and supports:
    - switch scope
    - switch back to builder
    - open current soul in `$EDITOR`
    - reset current scope content to default soul markdown (in-memory until save)
  - saves write directly to the appropriate soul markdown path

  Grounding/reuse:
  - reuses `imp-core::personality` helpers such as `default_soul_markdown`, `generated_tunable_line`, `replace_tunable_line`, `tunable_state_for_label`, and `soul_identity_text`
  - reuses `imp-core::resources` discovery for project soul path resolution
  - does not invent a parallel personality store

  Verification evidence:
  - `cargo check -p imp-cli` passed.
  - `cargo test -p imp-cli parse_chat_shell_command -- --nocapture` passed.
  - `cargo test -p imp-cli cycle_personality_tunable_updates_markdown -- --nocapture` passed.
  - built binary smoke run passed for top-level flow:
    - `tmpdir=$(mktemp -d); printf '2\n7\nq\nq\n' | env XDG_CONFIG_HOME="$tmpdir" target/debug/imp personality`
    - observed builder mode, tunable update, source-mode switch, discard path, and zero exit status
  - built binary smoke run passed for shell-local flow:
    - `tmpdir=$(mktemp -d); printf ':personality\nq\n:quit\n' | env XDG_CONFIG_HOME="$tmpdir" target/debug/imp --no-tools chat`
    - observed shell startup, personality flow opening, discard path, and return to shell prompt

  Current caveats / follow-up:
  - source mode is preview + `$EDITOR` handoff, not a full in-terminal text editor
  - smoke transcript capture still shows some prompt-ordering noise after quit, but functional behavior is verified
  - next natural CLI parity target is `imp setup` / `:setup`

  ---
  2026-04-09T08:44:26.575304+00:00
  Explicit durable decomposition update after implementation/verification:
  - `imp personality` now exists as a top-level terminal-native flow in `imp-cli`.
  - `:personality` / `/personality` now exists inside `imp chat`.
  - The first slice preserves the current real personality structure rather than inventing a new config model:
    - scope: global vs project
    - mode: builder vs source
    - builder tunables: Autonomy, Brevity, Caution, Warmth, Planning
    - source mode: preview + `$EDITOR` handoff + reset-to-default + save/discard
  - The flow is file-backed using the existing soul markdown model and reuses `imp-core::personality` and `imp-core::resources` helpers.

  Verification state to preserve:
  - crate-scoped checks/tests passed
  - built-binary smoke runs passed for both:
    - top-level `imp personality`
    - shell-local `:personality` inside `imp chat --no-tools`

  Boundary to preserve:
  - source mode is intentionally preview + editor handoff in this first slice, not a full in-terminal text editor.
labels:
- imp
- cli
- implementation
- personality
parent: '50.10'
kind: epic
attempt_log:
- num: 1
  outcome: abandoned
  agent: imp
  started_at: '2026-04-09T08:38:22.062477Z'
  finished_at: '2026-04-24T05:37:11.281426Z'
---

After `imp settings`, implement `imp personality` and `:personality` as real terminal-native flows. Ground this in the current capability breadth already inspected from `imp/crates/imp-tui/src/views/personality.rs`.

Required parity goals:
1. preserve builder mode with tunables like autonomy, brevity, caution, warmth, and planning
2. preserve source editing mode for the underlying soul/profile content
3. preserve global vs project scope handling
4. make the flow usable without fullscreen UI
5. allow optional richer handoff to editor/TUI when useful, but keep CLI parity first-class

Keep this unit focused on the personality surface only; do not expand into settings/setup here.

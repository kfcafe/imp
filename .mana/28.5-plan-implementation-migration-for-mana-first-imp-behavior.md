---
id: '28.5'
title: Plan implementation migration for mana-first imp behavior
slug: plan-implementation-migration-for-mana-first-imp-behavior
status: open
priority: 2
created_at: '2026-04-07T04:45:00Z'
updated_at: '2026-04-09T17:06:50.845108Z'
notes: |-
  ---
  2026-04-09T05:22:46.898344+00:00
  Migration input from 28.1: implementation should make imp aggressive about durable structure capture and conservative about consequential commitment. The roadmap should account for (1) doctrine/prompt updates that instruct conversation-time mana externalization, (2) runtime behavior for automatic unit/note/decision creation and update during planning turns, (3) confirmation thresholds for high-impact scope/architecture/ownership choices, and (4) user-visible between-turn mana delta summaries, likely informed by 28.4.

  ---
  2026-04-09T05:23:28.439288+00:00
  2026-04-09 — decomposition grounded from 28.1

  Outputs now available from 28.1:
  - automatic externalization triggers for planning/design conversations
  - artifact mapping across epic / child job / notes / decisions
  - autonomy threshold: auto-capture low-risk structure, ask only for consequential scope/architecture/execution choices
  - between-turn rule: update mana first when durable planning state changed, then summarize the delta in the reply

  Immediate follow-on decomposition that is concrete enough now:
  1. Patch `imp/crates/imp-core/src/system_prompt.rs` so the runtime doctrine explicitly teaches conversation-time mana externalization, artifact mapping, confirmation thresholds, and between-turn delta behavior.
  2. Update `imp/.imp/skills/mana/SKILL.md` and `imp/.imp/skills/mana-delegation/SKILL.md` so the skill guidance matches 28.1 and does not frame mana only as a late orchestration/delegation tool.

  Still blocked / not yet decomposed into worker-ready implementation units:
  - runtime enforcement or pre-reply hooks for mana sync and delta reporting, because 28.4 is still the spec unit for UI/runtime affordances and this unit should not guess the interaction surface before that closes
  - any mana model/API changes that depend on 28.2 or 28.3 boundary/memory outcomes

  Sequencing proposal:
  - after 28.2 closes, prompt + skill wording patches can proceed as independent implementation jobs
  - after 28.4 closes, create a separate runtime/UI implementation job for pre-reply mana sync and visible delta reporting
  - keep this work in root mana because it spans imp prompt doctrine, mana usage guidance, and the cross-project planning workflow contract

  ---
  2026-04-09T05:23:36.169768+00:00
  Additional migration input from 28.1: treat archived child 28.1 as completed policy input, but preserve its outputs in live planning by carrying them on parent 28 and downstream units. Implementation sequencing should explicitly separate (a) doctrine/prompt wording, (b) runtime trigger detection and safe append-style unit/note/decision mutations, (c) confirmation policy for consequential commitments, and (d) between-turn delta reporting/review surfaces.

  ---
  2026-04-09T05:23:49.875910+00:00
  New required input from 28.2: migration planning should be organized around replacing mana-side prompt rendering with a typed/structured execution bundle seam. Explicit long-term removals from the mana boundary: final `system_prompt` rendering, final `user_message` rendering, generic runtime coaching prose, and live file-to-prompt embedding. Explicit imp-owned assembly inputs to preserve: identity/personality/soul, tool registry/mode guidance, AGENTS.md/project context, session/personal memory, environment, guardrails, and live file/code-intelligence reads. Acceptable layering to retain during migration: imp-local projection types over canonical durable data and UX-local mirrored run state.

  ---
  2026-04-09T05:26:46.984460+00:00
  2026-04-09 — decomposition added from the 28.2 boundary-spec session. Immediate durable follow-ons now worth tracking explicitly: (1) a shared-contracts spec that turns the abstract `ExecutionBundle` / `WorkerResult` boundary into a concrete field-level contract and exclusion list, grounded in current `mana-core/src/prompt.rs`, `imp-core/src/system_prompt.rs`, and `imp-core/src/mana_worker.rs`; (2) a migration-sequencing spec for replacing mana-side final prompt rendering with imp-owned final assembly while preserving `mana context` as a human/operator view; and (3) a root-mana traceability repair for the missing 28.1/28.2/28.3 child-unit records so parent/dependency structure is truthful.

  ---
  2026-04-09T06:27:26.939168+00:00
  2026-04-09 — decomposition from 28.4 between-turn review spec has now been externalized. Agreed rollout order to preserve in the root graph: (1) imp-first runtime contract for a canonical turn-batched mana delta, replacing per-action review noise with one per-turn review object that includes explicit `no_change`; (2) imp compact between-turn review block plus an expanded textual local inspector for touched units/subtree/decisions/deps, asking only for consequential unresolved choices; (3) Wizard later consumes the same delta semantics as an optional review queue / focus-room entry / graph-highlighting layer, but must not be required for the first usable version. Concrete follow-on child jobs should track these phases separately so imp can ship the mandatory review affordance before any Wizard dependency.

  ---
  2026-04-09T06:29:29.217962+00:00
  2026-04-09 — Added concrete follow-on decomposition from 28.4 between-turn review spec:
  - 28.5.6 `Implement turn-scoped mana review packet aggregation in imp-core` — runtime-side aggregation unit grounded in `imp-core/src/agent.rs`, `tools/mod.rs`, and `tools/mana.rs`; goal is to introduce an explicit turn-level packet/state (`no_change` / `changed` / `needs_decision`) and stop relying on last-write-wins per-call widget updates as the durable turn story.
  - 28.5.7 `Render between-turn mana review packets across imp TUI and CLI/RPC surfaces` — presentation unit depending on 28.5.6; uses existing `set_widget`/`set_status` surfaces in `imp-tui` and `imp-cli` to show one compact post-turn summary and ask only for consequential unresolved choices.
  - 28.5.8 `Design Wizard as a richer consumer of between-turn mana review packets` — later design/spec unit for canvas/card/graph inspection downstream of the imp-first implementation, explicitly not a v1 dependency.

  Sequencing implied by this decomposition:
  1. land the imp-core packet/accumulator first
  2. wire compact rendering across imp surfaces second
  3. only then pursue richer Wizard-side graph review consumption

  ---
  2026-04-09T06:30:24.957540+00:00
  Reconciled overlap after externalizing the 28.4 follow-on plan. Existing spec units already covered the upstream design decomposition:
  - 28.5.3 = canonical turn-batched mana delta contract
  - 28.5.4 = imp compact between-turn review block/textual inspector
  - 28.5.5 = Wizard review-queue/focus-room consumer

  So the durable implementation-oriented additions are now:
  - 28.5.6 depends on 28.5.3 and owns imp-core runtime aggregation / turn-scoped packet implementation
  - 28.5.7 depends on both 28.5.4 and 28.5.6 and owns rendering across imp TUI and CLI/RPC surfaces
  - redundant Wizard unit 28.5.8 was deleted in favor of the pre-existing 28.5.5

  Clean sequencing after reconciliation:
  1. 28.5.3 contract spec
  2. 28.5.4 imp UI spec and 28.5.5 Wizard downstream spec
  3. 28.5.6 imp-core aggregation implementation
  4. 28.5.7 imp surface rendering implementation

  ---
  2026-04-09T08:52:23.307955+00:00
  2026-04-09 decomposition update after 28.5.1 implementation: the mana-first migration now has a clearer staged split. `28.5.1` is the prompt-doctrine layer and is implemented/verified in `imp-core/src/system_prompt.rs`. `28.5.2` remains the skills-layer wording follow-on so skill guidance matches the prompt doctrine. `28.5.3` / `28.5.4` / `28.5.6` / `28.5.7` remain the runtime/UI follow-ons for canonical between-turn mana delta contracts, aggregation, and rendering. This keeps prompt wording, skill wording, and runtime enforcement/reporting as separate durable artifacts rather than collapsing them into one unit.

  ---
  2026-04-09T09:22:28.612284+00:00
  2026-04-09 conversation-time externalization follow-up:
  - Completed the skill-guidance alignment work for the already-listed child `28.5.2 Update mana skills for conversation-time externalization policy`, but that child currently does not resolve via `mana show` even though it appears in the root index.
  - Durable decomposition captured by the doc update:
    1. Broaden `imp/.imp/skills/mana/SKILL.md` triggers so planning/design conversations that produce durable structure load the mana skill, not only larger execution work.
    2. Add concise artifact-selection guidance covering epic vs child job vs notes vs decisions, including preferring notes over noisy placeholder units while planning is still incomplete.
    3. Add confirmation policy: do not ask permission merely to capture plan artifacts; do ask for consequential scope, architecture, destructive, or execution choices.
    4. Add between-turn review-loop guidance: batch related durable changes, update mana before the substantive planning reply when needed, and summarize the mana delta briefly in chat.
    5. Mirror the relevant policy into `imp/.imp/skills/mana-delegation/SKILL.md` without duplicating the full mana skill or weakening its decomposition/worker-handoff focus.
  - Verification used for the doc slice: `grep -q "Do not ask permission merely to capture plan artifacts" imp/.imp/skills/mana/SKILL.md && grep -q "between-turn" imp/.imp/skills/mana/SKILL.md && grep -q "conversation-time" imp/.imp/skills/mana-delegation/SKILL.md`
  - Result: both skill docs now explicitly describe conversation-time externalization triggers, artifact selection, confirmation thresholds, and between-turn mana-delta review behavior.

  ---
  2026-04-09T09:24:23.040169+00:00
  2026-04-09 — 28.5.3 is now complete and defines the canonical turn-batched contract in `docs/rebuild/mana-turn-delta-contract.md`. Preserve this sequencing in follow-on work: (1) 28.5.4 defines imp-side rendering/interaction over the contract, not a new payload; (2) 28.5.5 defines Wizard as a later consumer of the same semantics; (3) 28.5.6 implements turn-scoped assembly in imp-core from mutating mana tool results; (4) 28.5.7 renders the resulting packet across TUI and CLI/RPC surfaces. Canonical field names now fixed by spec: `TurnManaDelta`, `touched_units`, `proposed_children`, `material_field_changes`, `notes_appended`, `decision_events`, `unresolved_consequential_choices`, and `no_change`.

  ---
  2026-04-09T09:24:57.402427+00:00
  28.5.3 is now closed with docs/rebuild/mana-turn-delta-contract.md as the canonical contract for between-turn review. Durable contract: imp assembles one presentation-neutral TurnManaDelta per assistant turn from successful mutating mana tool calls; state is explicit no_change | changed | needs_decision; canonical fields include anchor_unit, touched_units, proposed_children, material_field_changes, notes_appended, decision_events, and unresolved_consequential_choices. mana remains durable truth; imp owns turn-window assembly and summarization; Wizard is a later consumer of the same semantics, not a separate payload owner.

  ---
  2026-04-09T14:23:33.752427+00:00
  Durable decomposition for the between-turn mana review surface is now externalized under 28.5.7. Follow-on units created in root scope: 28.5.7.1 shared imp-core TurnManaDelta renderer plus review-mode config (`off` / `compact_on_change` / `pinned`), 28.5.7.2 imp-tui compact widget-tray block plus textual inspector, and 28.5.7.3 imp-cli/RPC rendering of the same semantics. 28.5.7.2 and 28.5.7.3 both depend on 28.5.7.1, and the whole rendering track continues to depend on 28.5.6 for canonical turn packet assembly. This preserves the plan outside chat and keeps the UI work explicitly line-oriented first, without requiring a graph browser or component widgets.

  ---
  2026-04-09T17:06:50.845098+00:00
  2026-04-09 — externalized the closed 28.5.5 Wizard-later review design into root mana because the child is now archived. Durable downstream plan from `docs/rebuild/wizard-mana-review-queue.md`: Wizard remains a later optional consumer of canonical `TurnManaDelta`, not a separate payload owner and not a dependency for the first usable review flow. The later Wizard slice should project canonical turn fields into (1) review queue entries, (2) focus-room entry points, (3) touched-cluster canvas highlights, and (4) unresolved-decision badges, while reading broader branch context directly from mana graph state. Minimum runtime feed remains narrow: one frozen completed-turn delta plus ordering metadata; everything else should come from direct mana reads. Preserve sequencing: imp-first contract/rendering stays mandatory in 28.5.6/28.5.7; any Wizard follow-on must stay additive, must not invent Wizard-only semantics, and must not assume a full live backend.
labels:
- architecture
- imp
- mana
parent: '28'
dependencies:
- '28.1'
- '28.2'
- '28.3'
- '28.4'
verify: test -n "migration-plan"
kind: job
---

Goal: turn the boundary/planning/memory/UI decisions into an implementation roadmap across mana, imp, and possibly Wizard.

Current state: the high-level direction is emerging, but it still needs sequencing into concrete implementation units.

Steps:
1. Gather outputs from 28.1–28.4.
2. Define phased implementation order.
3. Separate prompt/runtime changes from mana model/API changes and UI changes.
4. Produce worker-ready child units for actual implementation.

Files:
- units 28.1 through 28.4
- relevant code areas identified by those units

In scope:
- sequencing
- dependency ordering
- implementation unit decomposition

Out of scope:
- doing the implementation

Do not:
- start implementation before the architecture decisions are written down

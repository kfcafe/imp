---
id: '45.10.1'
title: Draft compact mana-platform target architecture using the agreed ontology
slug: draft-compact-mana-platform-target-architecture-us
status: open
priority: 1
created_at: '2026-04-16T05:31:07.317438Z'
updated_at: '2026-04-16T05:31:07.317438Z'
labels:
- architecture
- naming
- mana
- imp
- platform
- planning
parent: '45.10'
verify: cd /Users/asher/tower && test -n "mana-platform-target-architecture-tracked"
kind: job
---

Goal: write a short target-architecture document that uses the newly agreed future-facing vocabulary consistently.
Current state: naming is now converging on `mana` as the platform and `imp` as the agent, with `runtime`, `graph`, `extension`, `action`, and `task` as the core internal terms. The architecture split and migration target still need a compact document that uses those names consistently.

Produce:
- a compact architecture note describing `mana` as platform/substrate
- `imp` as agent + default human-facing environment
- `runtime` and `graph` as the core internal layers
- `extension` as the main packaged extensibility term
- how `wizard`, `familiar`, and the broader /agents direction sit on mana
- what this implies for future refactor direction

In scope:
- vocabulary-consistent target architecture
- cross-project framing
- explicit role split for mana, imp, wizard, familiar

Out of scope:
- implementation detail
- code changes
- full migration sequence

Do not:
- reintroduce `shell` as a core term unless required
- collapse `mana` back to only the durable graph layer
- use `module` or `capability` as the preferred main terms without an explicit reason

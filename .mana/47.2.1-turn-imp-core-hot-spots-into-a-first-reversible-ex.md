---
id: '47.2.1'
title: Turn imp-core hot spots into a first reversible extraction sequence
slug: turn-imp-core-hot-spots-into-a-first-reversible-ex
status: open
priority: 1
created_at: '2026-04-08T10:20:25.421481Z'
updated_at: '2026-04-08T10:20:25.421481Z'
labels:
- imp
- imp-core
- decomposition
- analysis
parent: '47.2'
verify: test -n "analysis-only"
kind: job
---

Goal: convert the imp-core decomposition epic into the first realistic extraction order. Current state: we know the biggest hot spots, but not yet the safest first cuts.

In scope:
- inspect the highest-risk large files in imp-core
- identify extraction seams that are behavior-preserving and reversible
- recommend whether each seam should become a new crate or module first
- define verify commands for the first extraction slices

Out of scope:
- landing the full refactor

Important decisions to resolve explicitly:
1. should `agent.rs` split by internal modules first or by new crate boundaries immediately?
2. should `tools/mod.rs` support helpers move before or after registry/tool traits are isolated?
3. should `mana_worker.rs` move only after shared contracts exist?
4. which extraction slices are too risky without adapter layers?

Suggested follow-ups:
- split `agent.rs` into turn loop vs tool execution coordination
- split `tools/mod.rs` into contracts/registry vs file/output helpers
- isolate worker runtime into a dedicated module/crate

Files to read:
- `crates/imp-core/src/agent.rs`
- `crates/imp-core/src/tools/mod.rs`
- `crates/imp-core/src/session.rs`
- `crates/imp-core/src/mana_worker.rs`
- `IMP_DEEP_REVIEW.md`
- `imp_rebuild_strategy.md`

Done when:
- the first two or three extraction moves are explicit enough to implement safely
- the verify strategy for each first slice is written down

---
id: '275.9'
title: Research unofficial Cursor provider support for imp
slug: research-unofficial-cursor-provider-support-for-im
status: open
priority: 3
created_at: '2026-04-27T18:16:20.134962Z'
updated_at: '2026-04-27T21:46:09.521226Z'
acceptance: A Cursor feasibility note identifies local credential/API-key/browser-login paths, where official Cursor CLI stores credentials if inspectable, whether those credentials can safely route model requests, ToS/brittleness risks, and a go/defer recommendation. If viable, a follow-up implementation task exists with explicit safety boundaries and verify gates.
notes: |2

  ## Attempt 1 — 2026-04-27T21:46:09Z
  Exit code: 1

  ```

  ```
labels:
- imp
- oauth
- cursor
- research
- unofficial
- risk
parent: '275'
dependencies:
- '275.11'
verify: test -f docs/plans/cursor-provider-feasibility.md && rg -n "Cursor|CURSOR_API_KEY|credential|go|defer|risk" docs/plans/cursor-provider-feasibility.md
attempts: 1
history:
- attempt: 1
  started_at: '2026-04-27T21:46:09.469695Z'
  finished_at: '2026-04-27T21:46:09.521223Z'
  duration_secs: 0.051
  result: fail
  exit_code: 1
kind: job
---

Goal: Research unofficial Cursor provider support for imp and produce a safe go/defer recommendation.
Current state: User wants unofficial Cursor support if possible. Known web evidence says Cursor CLI supports browser login (`agent login`, `agent status`, `agent logout`) and API keys via `CURSOR_API_KEY` or `--api-key`. We have not found a Pi Cursor provider implementation. Parent decision permits unofficial Cursor only with user-consented stable credentials; no silent browser cookie scraping or hidden credential extraction.

Steps:
1. Create or update `imp/docs/plans/cursor-provider-feasibility.md`.
2. Inspect public/official docs already identified if web is available:
   - https://cursor.com/docs/cli/reference/authentication
   - https://cursor.com/docs/api
   Summarize only relevant auth/provider facts.
3. Inspect local environment safely:
   - Find Cursor CLI binary: `command -v cursor || command -v agent || true`.
   - Run only safe metadata/status/help commands such as `cursor --help`, `agent --help`, or `agent status` if available. Do not run login/logout or commands that mutate credentials.
   - Locate likely credential/config directories by names only (`~/.cursor`, Cursor app support dirs) and inspect file names/metadata/schema only. Do not print token values. If a file appears to contain secrets, record path and format at a high level without contents.
4. Search Pi repo for Cursor support: `rg -n -i "cursor" /Users/asher/pi-mono/packages/ai /Users/asher/pi-mono/packages/coding-agent`.
5. Determine possible integration routes:
   - official API key via `CURSOR_API_KEY`
   - importing official Cursor CLI browser-login credentials if stable and user-consented
   - unsupported/private scraping (should be rejected)
6. Write doc sections:
   - `## Cursor feasibility`
   - `### Official auth surface`
   - `### Local credential findings` (no secrets)
   - `### Provider routing unknowns`
   - `### Recommendation` with `go`, `research more`, or `defer`
   - `### Safety boundary`
7. If recommendation is `go`, create a follow-up implementation unit with exact files and verify gates. If not, record blockers and what evidence would change the decision.

In scope:
- Research, local metadata inspection, recommendation.

Out of scope:
- Do not implement Cursor provider in this unit.
- Do not mutate Cursor login state.
- Do not print/copy/exfiltrate tokens.
- Do not inspect browser cookies.

Do not:
- Use browser cookie scraping.
- Claim Cursor subscription model routing works without proving endpoint/request compatibility.
- Run destructive Cursor CLI commands.

---
id: '271.2'
title: Harden imp spawn and mana close/tool execution against indefinite waits
slug: harden-imp-spawn-and-mana-closetool-execution-agai
status: open
priority: 1
created_at: '2026-04-24T17:20:22.648701Z'
updated_at: '2026-04-24T17:40:05.884401Z'
notes: |-
  ---
  2026-04-24T17:22:01.243242+00:00
  Likely first hardening slice:
  1. Replace imp-core mana_worker inline verify shell execution with mana_core::ops::verify::run_verify_command so worker verify honors per-unit/config verify_timeout instead of waiting forever.
  2. Replace mana-cli MCP close handler's raw std::process::Command::output() verify path with canonical mana-core verify helper so close tool calls also honor timeout.
  3. Add focused regression tests proving timed-out verify returns a terminal failure/result instead of hanging.
  4. Follow-up if still needed: bound ad_hoc spawn wait around ImpSession::prompt_and_wait / tool execution, likely by adding a spawn-level timeout/cancel contract rather than relying on tools/providers to terminate themselves.

  ---
  2026-04-24T17:26:30.576064+00:00
  Starting implementation of the first hardening slice: route imp worker inline verify and mana MCP close verify through mana-core's timeout-aware verify helper, then add regression coverage for timed-out verify so stuck verify commands fail terminally instead of hanging.

  ---
  2026-04-24T17:30:51.556794+00:00
  Implemented first hardening slice.
  - imp-core mana_worker verify now resolves per-unit/config verify_timeout from .mana and runs through mana_core::ops::verify::run_verify_command inside spawn_blocking, so worker inline verify returns terminal failure on timeout instead of hanging forever.
  - mana-cli MCP close handler now uses the same canonical verify helper and timeout resolution before closing a unit.
  - Added regression tests for worker verify timeout messaging and MCP close verify timeout failure.
  - Verification passed: cargo check -p imp-core -p mana-cli; cargo test -p imp-core run_verify_command_ -- --nocapture; cargo test -p mana-cli mcp_close_unit_ -- --nocapture.
  Remaining concern: ad_hoc spawn still waits on full ImpSession completion, so a provider/tool that never terminates can still hang the spawn caller. That should be the next slice if user wants deeper hardening.

  ---
  2026-04-24T17:32:49.250200+00:00
  Continuing with second hardening slice: inspect ad_hoc spawn wait path and add a bounded termination contract so spawn cannot wait forever when an ImpSession/provider/tool never reaches a terminal event.

  ---
  2026-04-24T17:35:56.659678+00:00
  Implemented second hardening slice for ad_hoc spawn.
  - imp spawn tool now accepts timeout_secs for ad_hoc mode and defaults to 300s.
  - ad_hoc spawn now calls session.prompt(), then waits under tokio::time::timeout; on timeout it sends cancel(), gives the session a short grace wait, and returns a terminal tool error instead of waiting forever.
  - Added focused tests for timeout_secs schema/default parsing.
  - Verification passed: cargo check -p imp-core; cargo test -p imp-core tools::imp -- --nocapture.
  Remaining gap: this bounds the caller-side wait, but a deeply stuck provider/tool may still leave background work alive after the short cancel grace if it does not honor cancellation promptly. Fixing that likely needs lower-level cancellation/abort support in ImpSession/agent/tool execution.

  ---
  2026-04-24T17:38:16.930907+00:00
  Continuing with third hardening slice: inspect lower-level ImpSession/agent cancellation so timed-out ad_hoc spawn not only returns promptly but also forcefully tears down stuck work when graceful cancel does not complete.

  ---
  2026-04-24T17:40:05.884391+00:00
  Implemented third hardening slice for lower-level ad_hoc spawn teardown.
  - Added ImpSession::abort() to force-abort the in-flight agent task and mark the run cancelled.
  - ad_hoc spawn timeout path now attempts graceful cancel first, then force-aborts the session if wait does not finish within the short grace window.
  - Added regression test proving abort() makes wait() return Cancelled instead of hanging.
  - Verification passed: cargo check -p imp-core; cargo test -p imp-core abort_marks_wait_as_cancelled -- --nocapture; cargo test -p imp-core tools::imp -- --nocapture.
labels:
- imp
- mana
- spawn
- hang
- implementation
parent: '271'
verify: cd /Users/asher/tower && cargo check -p imp-core -p imp-cli -p mana-cli && cargo test -p imp-core -- --nocapture && cargo test -p mana-cli close -- --nocapture
kind: task
---

Goal: implement targeted hardening so spawn and mana close/tool flows terminate predictably instead of waiting forever.
Current state: blocked on 271.1 diagnosis. This unit is for the code fix once exact failure modes are confirmed.

Expected scope after diagnosis:
1. Add explicit completion/error/timeout handling in the implicated imp tool path(s).
2. Add cancellation or idle/total timeout boundaries where child/stream waits can block indefinitely.
3. Ensure failed or incomplete background runs are recorded as terminal states in run_store / worker output.
4. Add focused regression tests for the identified hang class.

Likely files:
- imp/crates/imp-core/src/tools/imp.rs
- imp/crates/imp-core/src/tools/mana.rs
- imp/crates/imp-core/src/imp_session.rs
- mana/crates/mana-cli/src/commands/run/mod.rs
- mana/crates/mana-cli/src/spawner.rs
- mana/crates/mana-cli/src/commands/close/verify.rs

Do not:
- change behavior beyond what is needed to prevent indefinite waits
- weaken success semantics by marking unfinished work as success

---
id: '28.1.5.3.1'
title: Rename headless unit-worker CLI surface to `imp work` with `imp run` compatibility alias
slug: rename-headless-unit-worker-cli-surface-to-imp-wor
status: closed
priority: 1
created_at: '2026-04-15T03:34:05.111908Z'
updated_at: '2026-04-23T05:24:38.262651Z'
notes: |-
  ---
  2026-04-15T03:41:16.856787+00:00
  Execution context from initial inspection before implementation:
  - `crates/imp-cli/src/lib.rs` currently defines only `Commands::Run` for the headless unit-worker CLI surface (around the `Commands` enum block where the `Run` variant is declared with `unit_id`, `mana_dir`, and `defer_verify`).
  - The main command dispatch also branches only on `Commands::Run` and forwards directly to `run_headless_mode(&cli, unit_id, mana_dir.as_deref(), *defer_verify)`.
  - `run_headless_mode()` is already the single shared headless worker entrypoint, so the clean CLI rename path is to add a new `Work` subcommand that reuses this exact function and keep `Run` as compatibility alias during migration.
  - Current user-facing docs still prefer `imp run` in `README.md` (top quick-start example and later integration section).
  - Existing `imp-cli` tests heavily cover model/provider resolution but do not yet cover `work`/`run` command parsing or alias compatibility; this unit should add focused parse/dispatch coverage rather than relying only on resolver tests.

  Implementation direction for this unit:
  1. Introduce a shared args struct or equivalent for headless unit-worker subcommands so `Work` and compatibility `Run` do not drift.
  2. Add `Work` as the preferred subcommand, keep `Run` as alias/compatibility path, and route both to `run_headless_mode()`.
  3. Update CLI help strings and README examples to prefer `imp work` while still mentioning `imp run` as transitional compatibility where helpful.
  4. Add focused `imp-cli` tests for subcommand parsing/help/compatibility plus a narrow doc grep in verify.

  ---
  2026-04-15T03:44:09.173637+00:00
  Implemented the first CLI surface rename pass.

  Changes made:
  - Added `HeadlessWorkArgs` in `crates/imp-cli/src/lib.rs` so the headless unit-worker subcommand args are shared.
  - Replaced the old `Commands::Run { ... }` variant with preferred `Commands::Work(HeadlessWorkArgs)` and added clap alias support via `#[command(alias = "run")]`.
  - Updated main command dispatch so both `imp work` and compatibility `imp run` route to the same existing `run_headless_mode()` path.
  - Updated README quick-start and mana integration examples to prefer `imp work`, with explicit note that `imp run` remains a compatibility alias during migration.
  - Added focused `imp-cli` tests:
    - `cli_parses_work_subcommand_for_headless_worker`
    - `cli_parses_run_as_alias_for_work_subcommand`
  - Added `Debug` derives to the clap subcommand enums to support the new parse tests.

  Verification evidence:
  - `cargo test -p imp-cli cli_parses_work_subcommand_for_headless_worker -- --nocapture` ✅
  - `cargo test -p imp-cli cli_parses_run_as_alias_for_work_subcommand -- --nocapture` ✅
  - `cargo test -p imp-cli resolve_model_sonnet_alias -- --nocapture` ✅
  - `cargo run -q -p imp-cli -- work --help` shows `Usage: imp work [OPTIONS] <UNIT_ID>` ✅
  - `cargo run -q -p imp-cli -- run --help` resolves through the compatibility alias and shows the same `imp work` help surface ✅

  Residual note:
  - The prebuilt `../target/debug/imp` binary was stale during verification, so live help verification was done through `cargo run -q -p imp-cli -- ...` instead.

  ---
  2026-04-15T03:47:23.133117+00:00
  Naming is reopened after the initial `imp work` pass. User raised a stronger candidate: `imp mana <id>` as the canonical single-unit worker CLI because it more clearly names imp as the runtime using mana. Before proceeding further, we should compare `imp work <id>` vs `imp mana <id>` vs possibly a namespaced form like `imp mana work <id>` for extensibility, then either keep, revise, or revert the current `imp work` implementation accordingly.

  ---
  2026-04-15T03:47:48.390766+00:00
  Loaded the `mana` skill per user instruction before continuing planning. Next planning step is to evaluate the reopened CLI naming question with mana-native artifact updates before making further surface changes or proceeding to 28.1.5.3.2.

  ---
  2026-04-15T04:31:03.848450+00:00
  Naming evaluation update: user argues `imp mana <id>` is still extensible because `imp mana status` simply uses a reserved verb in the first positional slot instead of a unit id. Agreed, provided the parser reserves a small set of words (`status`, `show`, `logs`, etc.) and treats any other first positional token as a unit id. This likely makes `imp mana <id>` the strongest current candidate for the canonical single-unit worker surface, with prior `imp work` work now looking transitional/revisable.

  ---
  2026-04-15T06:52:14.316530+00:00
  Proposed parser/migration shape if `imp mana <id>` becomes canonical:
  - Add a top-level `mana` subcommand under `imp-cli`.
  - Inside that namespace, reserve a small set of explicit verbs (`status`, `show`, `logs`, possibly `next`/`tree` later).
  - If the first positional token after `imp mana` is not a reserved verb, interpret it as a mana unit id and route to the existing single-unit worker path.
  - Initial compatibility/migration: keep `imp run <id>` as alias; likely remove/replace the temporary `imp work` spelling rather than carrying both long-term.
  - This yields a clean user story: `imp mana <id>` = imp works a mana unit, while future `imp mana status` / `imp mana show 12` can coexist in the same namespace.
  Open question still worth confirming before implementation: whether `imp mana show <id>` should eventually be a true CLI wrapper around native mana inspection or whether `imp mana` should remain narrowly focused on worker-oriented paths plus a few operator conveniences.

  ---
  2026-04-15T06:52:57.949646+00:00
  Revised implementation plan after naming direction hardened:
  1. Remove the temporary preference for `imp work` and pivot the CLI surface to `imp mana <id>` as canonical.
  2. Keep `imp run <id>` as the compatibility alias during migration.
  3. Treat `imp mana` as a namespace: reserve explicit verbs like `status`, `show`, and `logs`; otherwise parse the first positional token as a mana unit id and route to the existing headless worker path.
  4. Update help text, README examples, and focused parsing/help tests to reflect `imp mana <id>` as the preferred spelling.
  5. Leave imp-to-imp tool naming to 28.1.5.3.4 rather than coupling it to this CLI naming change.

  This unit should revise the just-landed `imp work` pass rather than layering another synonym on top.

  ---
  2026-04-15T07:12:40.473378+00:00
  Naming/runtime plan tightened after the latest discussion:
  - The operator-facing CLI should likely converge on an `imp mana ...` namespace, with `imp mana <id>` as the canonical single-unit worker entrypoint and reserved verbs like `run/status/show/logs` for broader mana-aware operations.
  - But this CLI namespace is not the primary mechanism agents should use internally. For 99% of agent tasks, native tools should be the common path.
  - Therefore this unit should focus on operator-facing grammar/migration only (parsing/help/docs/aliases) and should not drive internal agent execution toward bashing `imp`.
  - Internal execution should instead lean on the existing native `mana` tool and the native `imp` tool/runtime seams tracked under root 81.
  - Practical implication: once the CLI naming is settled, revise the temporary `imp work` implementation toward `imp mana ...`, but keep the runtime/auth refactor and tool-boundary work model-first/native-first rather than shell-command-first.

  ---
  2026-04-15T07:20:52.728261+00:00
  Mid-implementation checkpoint after pivoting away from the temporary `imp work` spelling:
  - `crates/imp-cli/src/lib.rs` now uses `HeadlessManaArgs` and a new `ManaNamespaceArgs` parser shape instead of `HeadlessWorkArgs`.
  - The top-level clap surface has been revised from `Commands::Work` to:
    - `Commands::Mana(ManaNamespaceArgs)` as the canonical operator-facing namespace
    - hidden `Commands::Run(HeadlessManaArgs)` as the compatibility alias path
  - Command dispatch now routes `imp mana <target>` through a namespace branch that reserves `status`, `show`, `logs`, and `run` for future mana-aware operator verbs; any other target is treated as a mana unit id and forwarded to the existing `run_headless_mode()` single-unit worker path.

  Remaining concrete steps for this unit:
  1. Add the `run_reserved_mana_namespace_command()` helper so reserved verbs fail clearly/legibly for now instead of compiling broken.
  2. Update README/help/examples from `imp work` to `imp mana <id>` and keep `imp run <id>` as the transitional compatibility spelling.
  3. Replace the current parsing tests with `imp mana <id>` / `imp run <id>` coverage and add a narrow reserved-verb behavior test if practical.
  4. Run focused `imp-cli` tests plus live `cargo run -q -p imp-cli -- mana ... --help` verification, then close this unit if clean.

  Constraint reminder: this unit is operator-facing grammar only; internal agent execution should remain native-tool-first rather than shelling out to the CLI.

  ---
  2026-04-15T07:22:50.312772+00:00
  Completed the CLI grammar pivot from the temporary `imp work` experiment to the `imp mana` namespace.

  Implemented changes:
  - Replaced the temporary `HeadlessWorkArgs` / `Commands::Work` shape in `crates/imp-cli/src/lib.rs` with:
    - `HeadlessManaArgs` for the compatibility alias path
    - `ManaNamespaceArgs` for the new operator namespace
    - `Commands::Mana(ManaNamespaceArgs)` as the canonical operator-facing surface
    - hidden `Commands::Run(HeadlessManaArgs)` as the transitional compatibility alias
  - Updated dispatch so:
    - `imp mana <unit-id>` routes to the existing `run_headless_mode()` single-unit worker path
    - reserved namespace verbs `status`, `show`, `logs`, and `run` are trapped and currently fail clearly with guidance to use native `mana ...` directly until those wrappers land
    - `imp run <unit-id>` still works through the compatibility alias path
  - Added `run_reserved_mana_namespace_command()` for the reserved-verb guidance/error path.
  - Replaced README examples and integration docs to prefer `imp mana 5.1` / `imp mana <unit-id>` instead of `imp work`.
  - Replaced the prior parse tests with the new focused coverage:
    - `cli_parses_mana_namespace_unit_target_for_headless_worker`
    - `cli_parses_run_as_compatibility_alias_for_headless_mana_worker`
    - `cli_parses_reserved_mana_namespace_verb_with_passthrough_args`
    - `reserved_mana_namespace_commands_error_clearly`

  Verification evidence:
  - `cargo test -p imp-cli cli_parses_mana_namespace_unit_target_for_headless_worker -- --nocapture` ✅
  - `cargo test -p imp-cli cli_parses_run_as_compatibility_alias_for_headless_mana_worker -- --nocapture` ✅
  - `cargo test -p imp-cli cli_parses_reserved_mana_namespace_verb_with_passthrough_args -- --nocapture` ✅
  - `cargo test -p imp-cli reserved_mana_namespace_commands_error_clearly -- --nocapture` ✅
  - `cargo run -q -p imp-cli -- mana --help` shows `Usage: imp mana [OPTIONS] <TARGET> [ARGS]...` and describes `imp mana <unit-id>` as the single-unit path ✅
  - `cargo run -q -p imp-cli -- run --help` still shows the compatibility alias path ✅
  - `cargo run -q -p imp-cli -- mana status` now fails clearly with guidance to use native `mana status` directly until the reserved wrapper lands ✅
  - README grep confirms the public examples now prefer `imp mana` ✅

  Residual follow-up:
  - Reserved verbs are intentionally placeholders for now; true wrappers for `imp mana status/show/logs/run` belong in later operator-surface work, not this unit.
  - Internal agent execution remains native-tool-first; this unit only changed operator-facing CLI grammar.
  ## Attempt 1 — 2026-04-15T07:22:50Z
  Exit code: 1

  ```
  error: unexpected argument 'resolve_model' found

  Usage: cargo test [OPTIONS] [TESTNAME] [-- [ARGS]...]

  For more information, try '--help'.
  ```


  ---
  2026-04-23T05:24:38.262627+00:00
  2026-04-23 resumed and reverified against current workspace state. The implementation is already present and the original unit verify failed only because the cargo invocation was malformed (`cargo test ... run_headless resolve_model` passes two names instead of one filter). Current evidence: README and CLI help text prefer `imp mana <unit-id>` while `imp run <unit-id>` remains the compatibility alias; there are no remaining `imp work` examples. Focused parser/behavior tests all pass: `cli_parses_mana_namespace_unit_target_for_headless_worker`, `cli_parses_run_as_compatibility_alias_for_headless_mana_worker`, `cli_parses_reserved_mana_namespace_verb_with_passthrough_args`, and `reserved_mana_namespace_commands_error_clearly`. Acceptance for this unit is satisfied; only the stale verify command needed correction.
labels:
- refactor
- cli
- ux
- naming
- worker-runtime
parent: '28.1.5.3'
verify: cd /Users/asher/imp && cargo test -p imp-cli run_headless resolve_model -- --nocapture && rg -n 'imp work <unit-id>|imp work 5\.1|imp run <unit-id>' README.md crates/imp-cli/src/lib.rs
checkpoint: '9e044bfdfe03769494318cdd2364fd97dcf585cd'
verify_hash: '95f56a6922944ece0aa890c2aa274be45792cb11049c6fbf31b7a32a957091c5'
attempts: 1
claimed_by: imp
claimed_at: '2026-04-15T03:40:39.275455Z'
history:
- attempt: 1
  started_at: '2026-04-15T07:22:50.355642Z'
  finished_at: '2026-04-15T07:22:50.408225Z'
  duration_secs: 0.052
  result: fail
  exit_code: 1
  output_snippet: |-
    error: unexpected argument 'resolve_model' found

    Usage: cargo test [OPTIONS] [TESTNAME] [-- [ARGS]...]

    For more information, try '--help'.
kind: job
paths:
- crates/imp-cli/src/lib.rs
- README.md
attempt_log:
- num: 1
  outcome: abandoned
  agent: imp
  started_at: '2026-04-15T03:40:39.275455Z'
autonomy_disposition:
  kind: blocked
  blockers:
  - verify_failed
  review: unknown
  approval: unknown
  verify: failed
  visibility: unknown
  attempt_pressure: within_budget
  risk: unknown
  provenance: mixed
  continuation_budget: 2
---

Goal: make the headless mana-unit worker CLI surface clearer by introducing `imp work <unit-id>` as the preferred command while retaining `imp run <unit-id>` as a compatibility alias during migration.

Current state:
- `imp run <unit-id>` is the headless mana-unit worker CLI command.
- The repo also uses `run` language for other runtime/orchestration concepts, which makes the UX and architecture harder to talk about.
- The user prefers `imp work <id>` as the clearer CLI surface.

Steps:
1. Inspect the clap command definitions and dispatch in `crates/imp-cli/src/lib.rs`.
2. Add a new preferred `work` subcommand with the same semantics as the current headless unit-worker path.
3. Keep `run` working as a compatibility alias during migration.
4. Update CLI help text and README examples so `work` is the preferred spelling and `run` is clearly transitional if the implementation supports visible deprecation guidance.
5. Add focused tests for parsing/dispatch compatibility.

Files:
- `crates/imp-cli/src/lib.rs` (modify — command definitions, dispatch, tests)
- `README.md` (modify — examples/help wording)

In scope:
- CLI naming and compatibility alias behavior
- user-facing wording for the headless worker surface

Out of scope:
- changing the underlying worker runtime semantics
- renaming unrelated runtime internals unless needed for help text/tests

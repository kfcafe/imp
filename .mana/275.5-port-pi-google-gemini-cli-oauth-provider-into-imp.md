---
id: '275.5'
title: Port Pi Google Gemini CLI OAuth provider into imp
slug: port-pi-google-gemini-cli-oauth-provider-into-imp
status: open
priority: 2
created_at: '2026-04-27T18:15:52.980376Z'
updated_at: '2026-04-27T19:22:02.770837Z'
acceptance: imp supports a new Google Gemini CLI / Cloud Code Assist OAuth route ported from Pi. Users can login via CLI/TUI, credentials refresh, selected google-gemini-cli models route through the correct provider, setup/model picker exposes the provider/models, and targeted tests pass. Verify includes tests for OAuth URL/scopes/PKCE callback parsing or refresh, provider registry/model metadata, and runtime auth resolution.
labels:
- imp
- oauth
- google
- gemini
- pi-parity
parent: '275'
dependencies:
- '275.11'
verify: cargo test -p imp-llm google_gemini_cli --lib && cargo test -p imp-cli google_gemini_cli --lib && cargo test -p imp-tui google_gemini_cli --lib && cargo check -p imp-cli -p imp-tui
kind: task
---

Goal: Implement Google Gemini CLI / Cloud Code Assist OAuth support in imp after 275.10 and 275.11 establish the factual map and sequence.
Current state: imp already supports Google Gemini API-key provider (`google`) and OAuth providers for Anthropic, OpenAI/ChatGPT Codex, and Kimi Code. Pi has a working reference implementation in `/Users/asher/pi-mono/packages/ai/src/utils/oauth/google-gemini-cli.ts` and provider request logic in `/Users/asher/pi-mono/packages/ai/src/providers/google-gemini-cli.ts`. This unit is implementation-only and should not revisit sequencing decisions.

Steps:
1. Read `imp/docs/plans/pi-provider-oauth-parity.md`, especially Google Gemini CLI rows and sequence notes.
2. Inspect Pi reference:
   - `/Users/asher/pi-mono/packages/ai/src/utils/oauth/google-gemini-cli.ts`
   - `/Users/asher/pi-mono/packages/ai/src/providers/google-gemini-cli.ts`
   Capture exact public client ID/redirect URI/scopes only if they are intended installed-app/client constants; do not introduce private secrets without review.
3. Inspect imp patterns:
   - `imp/crates/imp-llm/src/oauth/kimi_code.rs` (loopback/device-ish OAuth patterns and common headers style)
   - `imp/crates/imp-llm/src/oauth/chatgpt.rs` and/or `anthropic.rs` (token exchange/refresh pattern)
   - `imp/crates/imp-llm/src/auth.rs` (StoredCredential OAuth storage, refresh hooks, OAuthDisplayInfo)
   - `imp/crates/imp-llm/src/model.rs` (ProviderMeta/ModelMeta entries)
   - `imp/crates/imp-llm/src/providers/mod.rs` and Google/OpenAI-compatible providers (provider creation)
   - `imp/crates/imp-cli/src/lib.rs` (`run_login`, setup, aliases)
   - `imp/crates/imp-tui/src/views/login_picker.rs`, `welcome.rs`, and app login handling.
4. Add a new provider ID only if the plan recommends it, likely `google-gemini-cli`, distinct from API-key `google`.
5. Implement OAuth login/refresh:
   - loopback callback with PKCE
   - token exchange/refresh at `https://oauth2.googleapis.com/token`
   - scopes from Pi: cloud-platform, userinfo.email, userinfo.profile
   - project discovery/provisioning via Cloud Code Assist endpoint if required by provider request path
   - email/display metadata if available.
6. Add model metadata for the initial high-value Gemini CLI models from the plan/Pi inventory.
7. Implement request routing/provider behavior for the selected models using the correct Cloud Code Assist endpoint and headers from Pi. Keep this as small as possible.
8. Integrate `imp login google-gemini-cli` (and any accepted aliases), setup/login picker visibility, model picker visibility, and auth refresh resolution.
9. Add targeted tests covering:
   - provider/model registry entries
   - auth login URL/scopes/PKCE callback parsing or refresh logic with mocked HTTP where possible
   - auth resolution uses OAuth for `google-gemini-cli`
   - setup/model picker includes the provider/models.
10. Update docs/help if login/provider behavior is user-visible.

Files likely modified:
- `imp/crates/imp-llm/src/oauth/*.rs` (add google_gemini_cli module)
- `imp/crates/imp-llm/src/auth.rs`
- `imp/crates/imp-llm/src/model.rs`
- `imp/crates/imp-llm/src/providers/*`
- `imp/crates/imp-cli/src/lib.rs`
- `imp/crates/imp-tui/src/views/login_picker.rs`
- `imp/crates/imp-tui/src/views/welcome.rs`
- possibly `README.md`.

In scope:
- Google Gemini CLI / Cloud Code Assist only.

Out of scope:
- Google Antigravity, Z.AI, Cursor, GitHub Copilot.
- Broad auth-store refactors.
- Silent use of user Google ADC unless explicitly planned.

Do not:
- Print or log tokens.
- Hard-code private secrets beyond public installed-app OAuth client constants already validated in the plan.
- Break existing `google` API-key provider behavior.

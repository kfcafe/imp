---
id: '273.5.11'
title: Add official Pi dynamic-tools compatibility fixture
slug: add-official-pi-dynamic-tools-compatibility-fixtur
status: open
priority: 1
created_at: '2026-04-27T05:38:14.442599Z'
updated_at: '2026-04-27T05:44:56.798827Z'
notes: |-
  ---
  2026-04-27T05:44:56.798821+00:00
  Implemented official Pi dynamic-tools compatibility fixture:
  - Added a reduced vendored fixture based on official `@mariozechner/pi-coding-agent` `examples/extensions/dynamic-tools.ts`.
  - Fixture uses `import type { ExtensionAPI }`, `import { Type } from '@sinclair/typebox'`, `pi.on('session_start')`, `ctx.ui.notify`, `pi.registerCommand`, and dynamically registers `echo_session` during session_start.
  - Updated Bun bridge to record lifecycle handlers and fire `session_start` after init so dynamically registered tools are visible during inspect.
  - Added minimal extension context with `ui.notify/setStatus/custom` and `sessionManager.getBranch/getEntries` stubs; this also fixed color-palette's lifecycle restore path.
  Verification passed with Bun: `PATH="$HOME/.bun/bin:$PATH" cargo test -p imp-core typescript_extensions -- --nocapture` => 8 passed, including color-palette and dynamic-tools fixtures.
labels:
- imp
- pi
- typescript
- extensions
- fixture
- external
parent: '273.5'
verify: cd /Users/asher/imp && PATH="$HOME/.bun/bin:$PATH" cargo test -p imp-core dynamic_tools typescript_extensions -- --nocapture
kind: task
---

Goal: add an external/official Pi compatibility fixture so imp is tested against real Pi API examples, not only Asher's local extensions.

Preferred fixture:
- Official `@mariozechner/pi-coding-agent` example `examples/extensions/dynamic-tools.ts`.

Why this fixture:
- Uses `registerTool` with TypeBox schema.
- Uses `registerCommand`.
- Uses `pi.on("session_start")`.
- Exercises dynamic registration semantics and UI notifications.
- It is external/official, avoiding overfitting to local extensions.

Steps:
1. Vendor a reduced fixture into imp tests or fetch/use a stable fixture copy with attribution.
2. Start with a reduced version if full command/event semantics are not in the first support boundary.
3. Verify the current adapter behavior: what registers immediately, what is ignored/stubbed, and what fails.
4. Convert failure modes into specific follow-up tasks for command/event/UI compatibility.

Out of scope:
- Full Pi API parity in this task.
- Network-dependent tests unless the fixture is vendored.

Acceptance:
- There is a stable external Pi fixture test or documented fixture artifact that exercises at least one non-local Pi extension pattern.

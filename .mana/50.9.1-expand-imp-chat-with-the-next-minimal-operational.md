---
id: '50.9.1'
title: Expand imp chat with the next minimal operational command set
slug: expand-imp-chat-with-the-next-minimal-operational
status: closed
priority: 1
created_at: '2026-04-09T07:28:49.138893Z'
updated_at: '2026-04-24T05:37:29.452196Z'
notes: |-
  ---
  2026-04-09T07:29:46.337555+00:00
  Starting implementation. Plan: extend the existing `imp chat` proving slice in `imp/crates/imp-cli/src/main.rs` with the next minimal shell commands (`:status`, `:new`, `:resume`, `:compact`) while keeping the shell transcript-oriented and avoiding inline browsing behavior. Likely approach: extract a small helper for constructing/replacing the current chat `ImpSession` so `:new` and `:resume` can swap sessions cleanly without inventing a second shell-local session model. For `:compact`, prefer a clear not-yet-supported message unless there is a safe existing shell-path hook available through current public APIs.

  ---
  2026-04-09T07:33:18.566405+00:00
  Implementation progress on 2026-04-09:
  - Extended the new `imp chat` shell in `imp/crates/imp-cli/src/main.rs` with the next minimal operational commands:
    - `:status`
    - `:new`
    - `:resume`
    - `:compact`
  - Added slash-command compatibility for these via the shared parser (`/status`, `/new`, `/resume`, `/compact`).
  - Added `print_chat_status_detail()` for a concise detailed status dump.
  - Extracted `build_chat_session(cli, session_choice)` helper so shell session replacement for `:new` and `:resume` reuses `ImpSession` creation rather than inventing shell-local state.
  - `:new` now swaps in a fresh `ImpSession` using `SessionChoice::New`.
  - `:resume` now swaps in a continued `ImpSession` using `SessionChoice::Continue` for the current cwd.
  - `:compact` currently returns an explicit honest message that shell-native compaction is not wired yet and points users to `imp tui` + `/compact` for now.

  Verification evidence:
  - `cargo test -p imp-cli parse_chat_shell_command -- --nocapture` passed.
  - `cargo test -p imp-cli shell_session_choice -- --nocapture` passed.
  - `cargo check -p imp-cli` passed.
  - Manual smoke run passed in `--no-tools` mode:
    - `printf ':status\n:new\n:status\n:resume\n:compact\n:quit\n' | cargo run -q -p imp-cli -- --no-tools chat`
    - observed status output, fresh-session replacement, continue-recent replacement, and explicit compact placeholder messaging.

  Current caveats / follow-up:
  - `:resume` is intentionally minimal; it continues the most recent session for the cwd and does not yet provide browsing/search UI.
  - `:compact` is placeholder-only in shell pending a cleaner shared compaction helper.
  - existing dead-code warnings in `imp-cli` remain from older mana-unit helper code and viewer/TUI-adjacent paths; they do not block this proving slice.

  ---
  2026-04-24T05:37:29.452195+00:00
  Graph hygiene pass 7 2026-04-24: verify passed (`cargo check -p imp-cli`, parse_chat_shell_command tests, and shell_session_choice tests). Closing stale in_progress implementation unit.
labels:
- imp
- cli
- implementation
- chat-shell
parent: '50.9'
verify: cd /Users/asher/tower && cargo check -p imp-cli && cargo test -p imp-cli parse_chat_shell_command -- --nocapture && cargo test -p imp-cli shell_session_choice -- --nocapture
checkpoint: '4867b54f68ed3484e1eedbee3ff2d9230bce648f'
verify_hash: f2fc81e02139bd6a09341f65ff9c6b138313668d5bc030c4ab4fea2fb5550456
kind: epic
attempt_log:
- num: 1
  outcome: abandoned
  agent: imp
  started_at: '2026-04-09T07:29:46.321613Z'
  finished_at: '2026-04-24T05:36:58.514988Z'
---

Extend the new `imp chat` CLI shell with the next small set of operationally useful commands after the initial proving slice. Current state: `imp chat` now exists in `imp/crates/imp-cli/src/main.rs` with status line, `imp>` prompt, compact tool notices, terminal `UserInterface` adapter, and minimal commands `:help`, `:quit`, `:model`, and `:thinking` (plus `/command` compatibility). Goal: make the shell more usable without turning it into a browser or trying to reach full parity yet.

Implement the next command subset only:
1. `:status` — print current shell/runtime/session status in a concise form
2. `:new` — start a fresh session using the current shell path
3. `:resume` — continue the most recent session for the cwd (keep this minimal; do not build a full session browser here)
4. `:compact` — trigger the existing compaction path or a clear not-yet-supported message if the shell path cannot safely support it yet

Design constraints:
- keep the shell transcript-oriented and single-pane
- do not add browsing-heavy inline views; those belong in `imp view`
- keep `/status`, `/new`, `/resume`, `/compact` compatibility if the command parser already supports slash aliases
- prefer reusing `ImpSession` / `SessionManager` surfaces over inventing a second shell-local session model

Likely files:
- `imp/crates/imp-cli/src/main.rs`
- shared session/runtime helpers if a small extraction becomes obviously worthwhile

Verification should stay narrow:
- targeted `imp-cli` tests for command parsing / session choice behavior
- `cargo check -p imp-cli`
- one or more simple non-LLM smoke invocations that exercise the new commands in `--no-tools` mode

---
id: '28.1.5'
title: Make imp's native mana tool the clear first-class orchestration UX for mana run
slug: make-imps-native-mana-tool-the-clear-first-class-o
status: closed
priority: 1
created_at: '2026-04-08T05:36:39.866766Z'
updated_at: '2026-04-08T06:31:26.304003Z'
notes: |-
  ---
  2026-04-08T06:31:05.839566+00:00
  Aligned imp's native mana tool wording with the clarified product stack so it reads as the first-class orchestration UX rather than a thin pass-through.

  Changes in `imp/crates/imp-core/src/tools/mana.rs`:
  - Follow-up summaries now say `Native mana orchestration finished ...` instead of `mana run finished ...`
  - Follow-up summaries explicitly mention layering: `Orchestration ran through mana; worker runtime: <agent> · <model>.`
  - Background start text now says `Started native mana orchestration in background ...` and explains that mana coordinates the run and dispatches imp workers underneath
  - UI status/widget text now says `mana orchestration: running ...` / `orchestrating ...`
  - Runtime detail text now describes the stack: `native mana tool → mana orchestration → <agent> workers ...`
  - Failure/follow-up text now consistently uses `Native mana orchestration ...`
  - `run_state` output now says `Native mana orchestration <run_id> ...` and labels runtime as `Worker runtime: ...`
  - `evaluate` output now says `Native mana orchestration run ...` and labels runtime as `Worker runtime: ...`
  - Default runtime fallback label changed from `unknown-agent` to `imp-worker` in these native-tool UX strings where appropriate

  Tests updated in `imp/crates/imp-core/src/tools/mana.rs`:
  - background run start text assertion updated
  - follow-up completion text assertion updated
  - run_state/evaluate wording assertions updated

  Verification:
  - `cargo test -p imp-core tools::mana::tests::run_state_and_evaluate_report_native_run -- --exact --nocapture` ✅
  - `cargo test -p imp-core tools::mana -- --nocapture` ✅ (27 passed)
  - `cargo check -p imp-tui` ✅

  Scope note: this work focused on native mana tool wording and state surfaces in imp-core, which is where the primary orchestration UX lives today. No broader orchestration logic changed.
labels:
- ux
- imp
- mana-tool
- orchestration
- imp-core
- imp-tui
closed_at: '2026-04-08T06:31:26.304003Z'
close_reason: 'Aligned imp''s native mana tool messaging so it clearly presents the intended stack: native mana tool as orchestration UX, mana as the orchestration engine, and imp workers underneath. Verified with `cargo test -p imp-core tools::mana -- --nocapture` and `cargo check -p imp-tui`.'
parent: '28.1'
verify: cd /Users/asher/tower && cargo test -p imp-core tools::mana -- --nocapture && cargo check -p imp-tui
checkpoint: '6fc1f3b5f4b8e4ec160692c76e3fc757b7b549b3'
verify_hash: a5e802c521496a2b94d5cdd5099111d91040b2a647c80267fe3860543d860928
claimed_by: imp
claimed_at: '2026-04-08T06:27:00.875473Z'
is_archived: true
requires:
- '28.1.1'
- '28.1.2'
- '28.1.3'
kind: job
paths:
- imp/crates/imp-core/src/tools/mana.rs
- imp/crates/imp-tui/src/views/tools.rs
- imp/crates/imp-tui/src/views/sidebar.rs
- imp/docs
attempt_log:
- num: 1
  outcome: success
  notes: 'Aligned imp''s native mana tool messaging so it clearly presents the intended stack: native mana tool as orchestration UX, mana as the orchestration engine, and imp workers underneath. Verified with `cargo test -p imp-core tools::mana -- --nocapture` and `cargo check -p imp-tui`.'
  agent: imp
  started_at: '2026-04-08T06:27:00.875473Z'
  finished_at: '2026-04-08T06:31:26.304003Z'
---

Goal: align imp's native mana tool UX with the strengthened imp worker contract so the intended first-class user path is clean, visible, and explicitly layered.

Current state:
- The native mana tool in `imp/crates/imp-core/src/tools/mana.rs` is already the intended first-class orchestration UX inside imp sessions: it supports `run`, `run_state`, `evaluate`, `logs`, background runs, summaries, and widget/status updates.
- However, the current 28.1 decomposition over-emphasized direct human-facing `imp run <id>` polish. The clarified product intent is that imp's native mana tool is the primary first-class UX, with `mana run` as orchestrator and `imp run` as worker runtime.
- Once 28.1.1–28.1.3 land, the native mana tool should clearly surface that stack rather than looking like a thin pass-through.

Steps:
1. Read 28.1 notes plus 28.1.1–28.1.3 outputs.
2. Update native mana tool messaging, summaries, widget/status content, and follow-up guidance so it clearly presents itself as the first-class orchestration surface inside imp.
3. Ensure background run start, run-state, evaluate, and logs flows describe the worker/runtime stack accurately: native mana tool → mana orchestration → imp workers.
4. Add focused tests or output assertions in `imp/crates/imp-core/src/tools/mana.rs` for the clarified UX wording where practical.
5. Coordinate with 28.1.4 so docs distinguish primary native-tool UX from secondary direct `imp run` usage.

Files:
- /Users/asher/tower/imp/crates/imp-core/src/tools/mana.rs
- /Users/asher/tower/imp/crates/imp-tui/src/views/tools.rs
- /Users/asher/tower/imp/crates/imp-tui/src/views/sidebar.rs
- /Users/asher/tower/imp/docs/ or README if docs need a brief adjustment

In scope:
- native mana tool UX wording and summaries
- widget/status alignment
- focused tests for clarified messaging

Out of scope:
- orchestrator implementation changes already covered elsewhere
- direct worker runtime extraction itself

Do not:
- make the native mana tool feel secondary to direct CLI `imp run`
- blur the ownership boundary so imp appears to own multi-unit orchestration policy

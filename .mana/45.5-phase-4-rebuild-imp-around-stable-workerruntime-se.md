---
id: '45.5'
title: Phase 4 — rebuild imp around stable worker/runtime seams
slug: phase-4-rebuild-imp-around-stable-workerruntime-se
status: open
priority: 2
created_at: '2026-04-08T17:15:13.365020Z'
updated_at: '2026-05-08T09:33:40.223621Z'
acceptance: A phase epic exists with child jobs for worker boundary cleanup, runtime decomposition, tool-system decomposition, session/context separation, and runner integration sequencing.
notes: |-
  ---
  2026-04-08T17:32:33.142027+00:00
  Decision from user on 2026-04-08: the first local runner implementation should start inside `imp` as a local adapter, while the protocol remains Tower-level from the start. Imp decomposition and runner-integration jobs should target that migration path rather than assuming a root `runner/` crate immediately.

  ---
  2026-04-09T12:35:14.791813+00:00
  Boundary checkpoint from completed spec 45.5.2: treat `imp/crates/imp-core/src/mana_worker.rs` as the explicit canonical worker-runtime seam for mana-assigned single-unit execution inside `imp`. Durable split to preserve through Phase 4: shared contracts live in `tower_contracts::worker`; imp-local worker execution logic stays in `imp-core` (`load_assignment*`, task/context shaping, prefill assembly, assignment-scoped dependency summaries, outcome production). First narrow follow-on sequence after the contracts move: (1) keep `mana_worker.rs` as the runtime center, (2) keep CLI/tool surfaces as adapters around that center, and (3) remove or quarantine stale duplicate mana-unit parsing helpers still left in `imp-cli/src/main.rs` so they no longer read as a second runtime path.

  ---
  2026-04-09T13:22:01.107327+00:00
  Phase 4 decomposition checkpoint after 45.5.4: the first narrow code-move implied by `docs/rebuild/imp-worker-boundary-plan.md` is now landed in code. `imp/crates/imp-cli/src/main.rs` no longer contains the stale duplicate mana-unit parsing/loading helpers (`ManaUnit`, `load_mana_unit`, `find_mana_dir`, `parse_mana_unit`). Current intended reading to preserve through subsequent slices: `tower_contracts::worker` owns shared worker vocabulary, `imp/crates/imp-core/src/mana_worker.rs` remains the canonical single-unit worker-runtime seam, and `imp-cli` is a thin adapter around that runtime for `imp run <unit-id>`. Do not reintroduce CLI-local worker contract or parser ownership in follow-on slices; broader decomposition remains deferred to 45.5.1 / 45.5.3.

  ---
  2026-04-09T13:24:54.933010+00:00
  Execution delta externalized from the worker-boundary cleanup slice: imp/crates/imp-cli/src/main.rs no longer carries the stale duplicate mana-unit helper set under the old names (ManaUnit/load_mana_unit/find_mana_dir/parse_mana_unit), and test coverage now targets imp_core::mana_worker prompt/context helpers rather than a shadow CLI-side parser path. Durable fact recorded as unit 69. Residual blocker from the original verify command was unrelated workspace breakage in mana-core at the time of checking, with missing autonomy-related types/fields causing cargo check -p imp-cli to fail while compiling mana-core.

  ---
  2026-05-08T09:33:40.223617+00:00
  Brainstorm/update from agent-loop comparison: future imp loop redesign should not treat max turns as ordinary loop policy. User preference: agents should be able to work on a task as long as needed; safety should come from explicit state, budgets/leases/recovery, no-progress/blocker detection, and final-status semantics rather than a crude turn cap. Promising directions confirmed: turn state machine, separate loop policy from mechanics, explicit final status, tool-call planning, and durable recovery/checkpoints. Treat these as core design axes for the stable worker/runtime seams work.
labels:
- tower
- rebuild
- phase-4
- imp
parent: '45'
dependencies:
- '45.2'
- '45.3'
- '45.4'
- '45.11'
kind: epic
---

Goal: shrink authority concentration in `imp-core` and move `imp` onto the explicit contract + runner boundary. Current inspected state: `imp-core/src/agent.rs`, `tools/mod.rs`, `session.rs`, `tools/mana.rs`, `config.rs`, and `mana_worker.rs` are the main hot spots. This phase depends on the earlier contract and runner work.

---
id: '82.1'
title: Probe existing openai-codex OAuth path with model id gpt-5.4-pro
slug: probe-existing-openai-codex-oauth-path-with-model
status: open
priority: 2
created_at: '2026-04-13T05:12:23.643399Z'
updated_at: '2026-04-13T05:12:23.643399Z'
labels:
- imp
- openai
- oauth
- probe
parent: '82'
verify: test -f .mana/82.1-probe-existing-openai-codex-oauth-path-with-model.md && rg -q '^id:' .mana/82.1-probe-existing-openai-codex-oauth-path-with-model.md
kind: job
---

Goal: verify whether the existing ChatGPT OAuth-backed provider path in imp can successfully send a request with model id `gpt-5.4-pro`.

Current state:
- `imp/crates/imp-llm/src/providers/openai_codex.rs` already builds requests for the ChatGPT/Codex backend and resolves auth from stored `openai` / `openai-codex` OAuth credentials.
- The likely gating factor is OpenAI backend entitlement, not local code shape.

Steps:
1. Use a real `imp login openai` credential in a local environment.
2. Send a minimal request through the existing `openai-codex` path with model id `gpt-5.4-pro`.
3. Capture whether the backend accepts, rejects, or remaps the model.
4. Record the exact error or success evidence in the parent unit notes.

Files/context:
- `imp/crates/imp-llm/src/providers/openai_codex.rs` (read — current OAuth-backed request path)
- `imp/crates/imp-cli/src/main.rs` (read — provider resolution and auth selection)
- `imp/crates/imp-llm/src/model.rs` (read — current model registry)

In scope:
- entitlement probe
- evidence capture

Out of scope:
- changing product-visible model lists before confirmation
- adding unsupported private scraping or browser automation

Verify: a captured live result clearly showing acceptance or rejection of `gpt-5.4-pro` through the existing OAuth-backed backend.

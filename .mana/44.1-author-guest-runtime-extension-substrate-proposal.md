---
id: '44.1'
title: Author guest-runtime extension substrate proposal
slug: author-guest-runtime-extension-substrate-proposal
status: closed
priority: 1
created_at: '2026-04-07T16:18:54.333868Z'
updated_at: '2026-04-23T16:16:40.329619Z'
notes: |-
  ---
  2026-04-17T07:30:02.329992+00:00
  Boundary clarification from root planning: the user's 'native Rust, not Lua' statement applies to imp tool/worker execution (`imp run` / worker runtime path), not to replacing the current shipped Lua extension substrate. Keep the guest-runtime proposal explicit that execution/runtime routing for workers remains Rust-native while Lua stays the current extension path unless separately changed.

  ---
  2026-04-17T08:06:10.536661+00:00
  Starting step 6 from the agreed near-term order. Will inspect the current Rust/Lua extension wiring and the proposal-style precedent docs, then draft `docs/proposals/guest-runtime-extension-substrate.md` with the now-settled boundary: worker execution stays native Rust while Lua remains the current shipped extension path.

  ---
  2026-04-17T08:08:43.521993+00:00
  Drafted `docs/proposals/guest-runtime-extension-substrate.md`.

  What the proposal now does:
  - reframes the current extension architecture from "Lua support" to a host-owned guest-runtime substrate
  - keeps Lua as the first shipped guest runtime
  - makes Rust host ownership explicit for policy, tool registry, and runtime authority
  - records the settled boundary that `imp run` / worker execution stays native Rust and is not routed through Lua
  - introduces the extension manifest / host capability vocabulary as the future-facing substrate language without requiring a JS/TS choice now
  - ends with a phased implementation plan grounded in the currently inspected files

  Grounding used from inspected code:
  - `crates/imp-core/src/builder.rs`
  - `crates/imp-core/src/tools/extend.rs`
  - `crates/imp-lua/src/lib.rs`
  - `crates/imp-lua/src/loader.rs`
  - `crates/imp-lua/src/sandbox.rs`
  - `crates/imp-lua/src/bridge.rs`
  - style precedent from the existing proposals in `docs/proposals/`

  Verification passed:
  - doc existence/content verify from the unit
  - `cargo check -p imp-core -p imp-lua`

  ---
  2026-04-23T16:16:40.329604+00:00
  2026-04-23 resumed and reverified. The proposal doc already exists and the unit content gate passes unchanged: `docs/proposals/guest-runtime-extension-substrate.md` is present and contains the required framing (`guest runtime`, `extension manifest`, `Lua`, `Rust host`). The unit notes already record that the proposal was grounded in the inspected builder/extend/Lua runtime files and that `cargo check -p imp-core -p imp-lua` had passed when authored. No new code/doc change was required on resume; acceptance is satisfied and the unit can be closed.
labels:
- architecture
- docs
- imp
- extensions
- guest-runtime
parent: '44'
verify: cd /Users/asher/imp && test -f docs/proposals/guest-runtime-extension-substrate.md && rg -q 'guest runtime|guest runtimes' docs/proposals/guest-runtime-extension-substrate.md && rg -q 'extension manifest' docs/proposals/guest-runtime-extension-substrate.md && rg -q 'Lua' docs/proposals/guest-runtime-extension-substrate.md && rg -q 'Rust host' docs/proposals/guest-runtime-extension-substrate.md
kind: job
paths:
- docs/proposals/guest-runtime-extension-substrate.md
- crates/imp-core/src/builder.rs
- crates/imp-core/src/tools/extend.rs
- crates/imp-lua/src/lib.rs
- crates/imp-lua/src/loader.rs
- crates/imp-lua/src/sandbox.rs
- crates/imp-lua/src/bridge.rs
---

Goal: author the main design proposal for a guest-runtime extension substrate in imp.
Current state: imp has a runtime-specific Lua path (`lua_tool_loader` in `crates/imp-core/src/builder.rs`, `crates/imp-lua/src/*`), and the current `extend` tool is focused on skills/Lua reference rather than general extension management.

Steps:
1. Write a proposal doc in `docs/proposals/` that defines the architectural shift from "Lua extensions" to "guest runtimes".
2. Specify core concepts: Rust host ownership, guest runtime interface, extension manifest, permissions/capabilities, durable extension lifecycle, and migration path from current Lua support.
3. Ground the proposal in inspected files: `crates/imp-core/src/builder.rs`, `crates/imp-core/src/tools/extend.rs`, `crates/imp-lua/src/lib.rs`, `crates/imp-lua/src/loader.rs`, `crates/imp-lua/src/sandbox.rs`, `crates/imp-lua/src/bridge.rs`.
4. Include explicit boundaries: what remains in Rust, what moves behind guest runtimes, and what not to do yet.
5. End with a phased implementation plan that future workers can decompose into code changes.

Files:
- `docs/proposals/guest-runtime-extension-substrate.md` (create — primary design note)
- `crates/imp-core/src/builder.rs` (read — current runtime-specific hook)
- `crates/imp-core/src/tools/extend.rs` (read — current authoring scope)
- `crates/imp-lua/src/lib.rs` (read — existing extension bootstrap)
- `crates/imp-lua/src/loader.rs` (read — extension discovery/loading)
- `crates/imp-lua/src/sandbox.rs` (read — runtime state and policy surface)
- `crates/imp-lua/src/bridge.rs` (read — host API)
- `docs/proposals/imp-memory-architecture-and-mana-boundary.md` (read — proposal style precedent)
- `docs/proposals/mana-aware-runtime-context-read-path.md` (read — proposal style precedent)

Context:
- The proposal should preserve the working Lua extension path as the first guest runtime rather than replacing it.
- JS/TS should be treated as a future possible runtime, not a required part of phase 1.
- The key architectural move is language-agnostic host capabilities and policy enforcement in Rust.
- The design should explain why this is bigger than a new tool and how it changes the extension substrate.

In scope:
- architecture, terminology, boundaries, migration framing, implementation phases

Out of scope:
- code implementation
- runtime benchmarking
- choosing a specific JS engine now

Do not:
- frame the design as "replace Lua with TS"
- overload the proposal with speculative engine details
- leave worker-facing steps vague; the phases should be concrete
